# 开发需求：三周模拟盘观察增强方案 V2

## 实施进度（2026-06-13）

已完成：

1. 在现有 `data/crypto_trading.db` 上完成兼容迁移，不另建割裂数据库；
2. 新增 `schema_metadata`、`runs`、`market_scans`、`paper_plans`、`paper_events`、`paper_snapshots`；
3. 启用 WAL、`synchronous=NORMAL`、`foreign_keys=ON`、`busy_timeout=30000`；
4. 新增 `python main.py db init` 与 `python main.py db status`；
5. daily 改为单一 `daily_full` run，五个步骤共用同一个 `run_id`；
6. 现有 scan、paper trade 和 paper event 已幂等回填到观察表；
7. paper update 已实现每 plan 独立短事务、API 请求与写事务分离、前置状态检查、stop 不降低校验；
8. 已实现严格 4h close_time 校验及 `API_DELAY_SKIPPED`；
9. 每次受跟踪的 paper update 写入 `paper_snapshots`；
10. 报告与 dashboard 标注 `run_id`、`run_type` 和 SQLite 数据来源；
11. 新增 `paper db-summary`、`paper db-events`、`paper db-export`；
12. 新增 `paper cycle --run-type paper_4h_update`，仅更新现有计划并生成独立 4h 文件名；
13. 新增数据库、状态安全、未收盘 K 线、snapshot、WAL 并发与 CSV 导出测试；
14. 结构化事件已统一为 `PLAN_CREATED`、`RECLAIM_PENDING_SET`、`RECLAIM_CONFIRMED_ENTERED`、`TP2_HIT`、`EMA_TRAILING_STOPPED` 等复盘口径；
15. 4h 报告增加本次 `run_id` 的状态变化表、事件总数和 `API_DELAY_SKIPPED` 数量；
16. K 线未收盘或 API 延迟时只写 `API_DELAY_SKIPPED` 与 snapshot，不再改写 `paper_plans`；同一 plan、同一目标 kline 去重；
17. `market_scans` 自动继承 run 的 `config_hash` 并记录 `market_regime`，`scan_candidates` 与 `paper_plans` 同步继承该环境字段；
18. schema v2 为 `paper_plans` 补齐完整运行态字段，paper update、paper report 和 observation dashboard 已改为读取 `paper_plans/paper_events`；旧 `paper_trades/paper_trade_events` 仅作为兼容镜像保留；
19. 新增真实 4h cycle 集成测试和锁竞争失败审计测试：验证 4h cycle 不新增 scan/plan，只推进已有计划并写 run、event、snapshot、独立 report/dashboard；验证写锁等待超时后 run 留下 `failed` 与 `database is locked` 错误；
20. 新增 per-plan 事务故障注入测试：第一笔在 plan update 后、event insert 前失败时，plan/event/snapshot 全部回滚；第二笔仍继续成功推进，整轮最终明确失败而不伪装成功。

已准备但尚未启用 `scripts/paper_4h_update.bat`、`scripts/install_4h_paper_task.ps1` 和 `python main.py db stability --days 5`。安装脚本会把连续 5 天稳定性审计作为硬门槛，未通过时拒绝注册 `CryptoTrading_4H_PaperUpdate`。截至 2026-06-13 08:43 +08:00，审计仍为 `observed_day_count=0`、`ready_for_4h_task=false`；当天 20:05 尚未到，首个新版样本预计由 2026-06-13 20:05 的 daily 自动产生。门槛通过后由用户在管理员 PowerShell 中运行安装脚本。

## 一、背景与目标

当前项目已经完成加密货币自动交易系统的核心模块，包括：

1. 选币扫描；
2. 买入卖出策略；
3. 回测模块；
4. paper trading 模拟盘；
5. daily 自动运行；
6. markdown 格式的 market scan、paper report、observation dashboard。

当前 daily 任务每天北京时间 20:05 执行统一入口：

```text
python main.py daily --account demo
```

该入口在同一个 `daily_full` run 中顺序执行 scan、add-from-scan、paper update、paper report 和 observation dashboard。

三周模拟盘观察的目标不是简单看最终收益，而是验证：

1. 选币策略是否能持续发现候选机会；
2. 买入规则是否能正确处理 entry、reclaim、pending；
3. 卖出规则是否能正确触发 stop、TP1、TP2、EMA trailing stop；
4. paper trading 状态机是否能跨天稳定推进；
5. 定时任务、日志、报告、状态持久化是否稳定；
6. 三周后是否可以基于结构化数据复盘完整交易路径。

目前主要问题是：系统主要依赖 markdown 报告保存结果。如果未来增加每 4 小时 paper update，21 天内会产生约 126 次更新记录，仅靠 markdown 后期分析会非常困难。因此需要先引入 SQLite 数据库作为结构化主数据源，再在稳定观察后启用 4h update。

本次开发目标分两部分：

1. 构建 SQLite 数据库，作为 paper trading 三周观察的主数据源；
2. 在数据库稳定运行 5 天后，新增每 4 小时 paper update 任务，用于更精细追踪交易状态。

---

# 二、总体架构原则

## 2.1 数据分层

采用：

```text
SQLite 数据库 = 实验主数据源
Markdown 报告 = 人类可读展示层
日志文件 = 故障排查辅助信息
```

要求：

1. daily 和 paper update 的核心数据必须写入 SQLite；
2. markdown 报告继续生成，但不再作为唯一数据源；
3. report 和 dashboard 尽量从 SQLite 读取数据生成；
4. 三周后复盘应通过 SQL / pandas / db-summary 完成，而不是解析 markdown。

---

## 2.2 运行分层

保留当前 daily_full：

```text
每天 20:05：
scan
paper add-from-scan
paper update
paper report
observation-dashboard
```

未来新增 paper_4h_update：

```text
00:10
04:10
08:10
12:10
16:10
```

paper_4h_update 只执行：

```text
paper update
paper report
observation-dashboard
```

明确禁止 paper_4h_update 执行：

```text
scan
paper add-from-scan
```

原因：

1. 保持每日选币口径稳定；
2. 避免每 4 小时重复创建新计划；
3. 避免短周期噪音污染候选池；
4. 4h update 的目标是更新已有交易计划和持仓状态，而不是提高选币频率。

---

# 三、SQLite 数据库基础设施

## 3.1 数据库路径

推荐路径：

```text
data/paper_trading.db
```

如项目已有数据库或状态文件，需要兼容现有结构，避免破坏当前 daily 任务。

---

## 3.2 数据库初始化命令

新增命令：

```text
python main.py db init
python main.py db status
```

`db init` 负责：

1. 创建数据库文件；
2. 创建所有表；
3. 创建索引；
4. 设置 PRAGMA；
5. 写入或检查 schema version；
6. 重复执行不报错。

`db status` 负责输出：

1. 数据库路径；
2. schema version；
3. WAL 状态；
4. 表是否存在；
5. 最近 run；
6. 最近失败 run；
7. 当前开放计划数量。

---

## 3.3 SQLite 并发与锁处理

SQLite 是单文件数据库，未来 daily_full 和人工查询、4h update 可能出现读写冲突。因此必须增加并发保护。

数据库初始化时强制设置：

```sql
PRAGMA journal_mode=WAL;
PRAGMA synchronous=NORMAL;
PRAGMA foreign_keys=ON;
PRAGMA busy_timeout=30000;
```

Python 连接数据库时使用：

```python
sqlite3.connect(db_path, timeout=30)
```

要求：

1. 开启 WAL，提升读写并发能力；
2. 设置 30 秒 busy timeout，避免瞬间 `database is locked`；
3. 所有写入操作使用短事务，避免长时间占用锁；
4. 不允许在持有数据库写锁期间调用外部 API；
5. API 请求和数据计算应先完成，再开启事务写数据库；
6. 如果仍发生 database locked，日志必须明确记录 run_id 和当前步骤。

---

# 四、时间戳规范

## 4.1 数据库存储统一使用 UTC

所有数据库时间字段强制使用 ISO 8601 UTC 格式。

例如：

```text
2026-06-12T12:10:00Z
```

涉及字段包括但不限于：

```text
started_at
finished_at
created_at
updated_at
event_time
scan_time
snapshot_time
kline_time
closed_at
```

要求：

1. 数据库中不混用北京时间、本地时间、交易所时间；
2. API 返回 UTC 时保持 UTC；
3. Python 内部统一使用 timezone-aware datetime；
4. Markdown 报告和 dashboard 展示时再转换为北京时间；
5. 报告中建议同时显示 UTC 与北京时间，至少标题使用北京时间。

---

# 五、数据库表设计

## 5.1 runs：运行批次表

每次运行 daily_full、paper_4h_update 或手动任务，都写入 runs。

```sql
CREATE TABLE IF NOT EXISTS runs (
    run_id TEXT PRIMARY KEY,
    run_type TEXT NOT NULL,
    started_at TEXT NOT NULL,
    finished_at TEXT,
    status TEXT NOT NULL,
    config_hash TEXT,
    git_commit TEXT,
    log_path TEXT,
    error_message TEXT,
    created_at TEXT NOT NULL
);
```

字段说明：

```text
run_type:
  daily_full
  paper_4h_update
  manual
  backfill

status:
  running
  success
  failed
```

要求：

1. 命令启动时写入 `running`；
2. 成功后更新为 `success`；
3. 失败后更新为 `failed` 并写入 error_message；
4. run_id 必须唯一，可用 UTC 时间戳 + UUID。

---

## 5.2 market_scans：扫描批次表

每次执行 scan 写入一条记录。

```sql
CREATE TABLE IF NOT EXISTS market_scans (
    scan_id TEXT PRIMARY KEY,
    run_id TEXT NOT NULL,
    scan_time TEXT NOT NULL,
    market_regime TEXT,
    universe_size INTEGER,
    candidate_count INTEGER,
    buy_candidate_count INTEGER,
    watch_only_count INTEGER,
    risk_off_count INTEGER,
    config_hash TEXT,
    report_path TEXT,
    created_at TEXT NOT NULL,
    FOREIGN KEY(run_id) REFERENCES runs(run_id)
);
```

要求：

1. 仅 daily_full 写入；
2. paper_4h_update 不写入；
3. scan_id 唯一；
4. scan_time 使用 UTC。

---

## 5.3 scan_candidates：扫描候选明细表

每个 scan candidate 写入一行。

```sql
CREATE TABLE IF NOT EXISTS scan_candidates (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    scan_id TEXT NOT NULL,
    symbol TEXT NOT NULL,
    rank INTEGER,
    action TEXT,
    price REAL,
    volume REAL,
    market_regime TEXT,
    entry_low REAL,
    entry_high REAL,
    stop REAL,
    tp1 REAL,
    tp2 REAL,
    score REAL,
    reason TEXT,
    raw_json TEXT,
    created_at TEXT NOT NULL,
    FOREIGN KEY(scan_id) REFERENCES market_scans(scan_id),
    UNIQUE(scan_id, symbol)
);
```

要求：

1. 同一个 scan_id + symbol 不重复；
2. raw_json 保存暂未结构化的信息，避免丢数据；
3. 字段不足时允许为空。

---

## 5.4 paper_plans：模拟盘交易计划表

当 `paper add-from-scan` 创建模拟交易计划时写入。

```sql
CREATE TABLE IF NOT EXISTS paper_plans (
    plan_id TEXT PRIMARY KEY,
    source_scan_id TEXT,
    source_symbol TEXT NOT NULL,
    created_run_id TEXT,
    created_at TEXT NOT NULL,
    symbol TEXT NOT NULL,
    entry_low REAL,
    entry_high REAL,
    stop_initial REAL,
    stop_current REAL,
    tp1 REAL,
    tp2 REAL,
    status TEXT NOT NULL,
    created_reason TEXT,
    market_regime TEXT,
    raw_json TEXT,
    updated_at TEXT NOT NULL,
    closed_at TEXT,
    FOREIGN KEY(source_scan_id) REFERENCES market_scans(scan_id),
    FOREIGN KEY(created_run_id) REFERENCES runs(run_id),
    UNIQUE(source_scan_id, source_symbol)
);
```

建议状态：

```text
WATCHING
RECLAIM_PENDING
ENTERED
TP1_HIT
TP1_EMA_TRAILING_ACTIVATED
CLOSED
STOPPED
EXPIRED
INVALIDATED
```

要求：

1. 同一个 source_scan_id + source_symbol 不重复创建计划；
2. add-from-scan 重复执行不重复建计划；
3. stop_current 初始等于 stop_initial；
4. 状态只能通过合法状态机单向推进；
5. closed_at 使用 UTC。

---

## 5.5 paper_events：交易事件流水表

这是最关键的表。每次 paper update 发生状态变化或关键观察事件，写入 paper_events。

```sql
CREATE TABLE IF NOT EXISTS paper_events (
    event_id TEXT PRIMARY KEY,
    plan_id TEXT NOT NULL,
    run_id TEXT NOT NULL,
    event_time TEXT NOT NULL,
    event_type TEXT NOT NULL,
    symbol TEXT NOT NULL,
    price REAL,
    old_status TEXT,
    new_status TEXT,
    old_stop REAL,
    new_stop REAL,
    kline_time TEXT,
    reason TEXT,
    raw_json TEXT,
    created_at TEXT NOT NULL,
    FOREIGN KEY(plan_id) REFERENCES paper_plans(plan_id),
    FOREIGN KEY(run_id) REFERENCES runs(run_id),
    UNIQUE(plan_id, event_type, event_time)
);
```

事件类型建议：

```text
PLAN_CREATED
ENTRY_ZONE_TOUCHED
RECLAIM_PENDING_SET
RECLAIM_CONFIRMED_ENTERED
ENTERED
STOPPED
TP1_HIT
TP2_HIT
TP1_EMA_TRAILING_ACTIVATED
TP1_EMA_TRAILING_RAISED
EMA_TRAILING_STOPPED
CLOSED
EXPIRED
INVALIDATED
API_DELAY_SKIPPED
NO_STATE_CHANGE
```

要求：

1. 真正状态变化必须写 event；
2. `NO_STATE_CHANGE` 默认不大量写入，避免事件表膨胀；
3. `API_DELAY_SKIPPED` 用于记录 K 线未确认收盘导致跳过；
4. 同一 plan 的同类事件在同一 event_time 不重复；
5. 状态更新和事件写入必须处于同一个事务。

---

## 5.6 paper_snapshots：模拟盘状态快照表

每次 paper update 完成后，记录当前计划状态快照。

```sql
CREATE TABLE IF NOT EXISTS paper_snapshots (
    snapshot_id TEXT PRIMARY KEY,
    run_id TEXT NOT NULL,
    snapshot_time TEXT NOT NULL,
    plan_id TEXT NOT NULL,
    symbol TEXT NOT NULL,
    status TEXT NOT NULL,
    current_price REAL,
    entry_price REAL,
    stop_current REAL,
    tp1 REAL,
    tp2 REAL,
    tp1_hit INTEGER,
    ema_trailing_active INTEGER,
    ema_stop REAL,
    unrealized_pnl REAL,
    realized_pnl REAL,
    holding_hours REAL,
    raw_json TEXT,
    created_at TEXT NOT NULL,
    FOREIGN KEY(run_id) REFERENCES runs(run_id),
    FOREIGN KEY(plan_id) REFERENCES paper_plans(plan_id),
    UNIQUE(run_id, plan_id)
);
```

要求：

1. 每次 paper update 后写入 snapshot；
2. 同一个 run_id + plan_id 不重复；
3. 4h update 下也必须写入；
4. 三周观察期内建议尽量完整记录开放计划和当天有事件的计划。

---

# 六、索引设计

为三周后复盘和 db-summary 提供基础性能保障。

必须创建以下索引：

```sql
CREATE INDEX IF NOT EXISTS idx_runs_type_started
ON runs(run_type, started_at);

CREATE INDEX IF NOT EXISTS idx_runs_status_started
ON runs(status, started_at);

CREATE INDEX IF NOT EXISTS idx_scan_candidates_symbol
ON scan_candidates(symbol);

CREATE INDEX IF NOT EXISTS idx_scan_candidates_scan_action
ON scan_candidates(scan_id, action);

CREATE INDEX IF NOT EXISTS idx_plans_symbol_status
ON paper_plans(symbol, status);

CREATE INDEX IF NOT EXISTS idx_plans_status_updated
ON paper_plans(status, updated_at);

CREATE INDEX IF NOT EXISTS idx_event_plan_type
ON paper_events(plan_id, event_type);

CREATE INDEX IF NOT EXISTS idx_event_type_time
ON paper_events(event_type, event_time);

CREATE INDEX IF NOT EXISTS idx_event_run
ON paper_events(run_id);

CREATE INDEX IF NOT EXISTS idx_snapshot_run_plan
ON paper_snapshots(run_id, plan_id);

CREATE INDEX IF NOT EXISTS idx_snapshot_plan_time
ON paper_snapshots(plan_id, snapshot_time);
```

---

# 七、状态机安全要求

## 7.1 状态机实现原则

状态流转逻辑优先在 Python 应用层实现，不建议初期使用 SQLite Trigger。

原因：

1. 当前策略仍处于实验期，状态规则可能调整；
2. Trigger 会让业务逻辑分散，后续排查困难；
3. Python 层更容易写测试；
4. SQLite 层通过唯一约束、外键、索引、CHECK 兜底即可。

---

## 7.2 合法状态流转

需要在代码中定义合法状态流转表，例如：

```python
ALLOWED_TRANSITIONS = {
    "WATCHING": {
        "RECLAIM_PENDING",
        "ENTERED",
        "EXPIRED",
        "INVALIDATED",
        "STOPPED",
    },
    "RECLAIM_PENDING": {
        "ENTERED",
        "EXPIRED",
        "INVALIDATED",
        "STOPPED",
    },
    "ENTERED": {
        "TP1_HIT",
        "STOPPED",
        "CLOSED",
    },
    "TP1_HIT": {
        "TP1_EMA_TRAILING_ACTIVATED",
        "TP2_HIT",
        "EMA_TRAILING_STOPPED",
        "CLOSED",
    },
    "TP1_EMA_TRAILING_ACTIVATED": {
        "TP1_EMA_TRAILING_ACTIVATED",
        "TP2_HIT",
        "EMA_TRAILING_STOPPED",
        "CLOSED",
    },
    "CLOSED": set(),
    "STOPPED": set(),
    "EXPIRED": set(),
    "INVALIDATED": set(),
}
```

说明：

1. 终态包括 CLOSED、STOPPED、EXPIRED、INVALIDATED；
2. 终态不得回滚；
3. 已经 TP1_HIT 的计划不得回到 ENTERED；
4. stop_current 只能在合理情况下抬高，不得降低；
5. EMA trailing stop raised 可以保持在同一状态，但必须写事件。

---

## 7.3 UPDATE 前置状态条件

执行数据库更新时必须附带当前状态条件，防止旧数据覆盖新状态。

例如：

```sql
UPDATE paper_plans
SET status = ?, stop_current = ?, updated_at = ?
WHERE plan_id = ?
  AND status = ?;
```

如果 affected rows = 0，则说明状态已被其他流程推进或数据已变化，应：

1. 不强行覆盖；
2. 记录 warning；
3. 重新读取当前状态；
4. 必要时跳过该 plan。

---

## 7.4 细粒度事务

每个 plan 的状态推进应使用独立事务：

```text
BEGIN
  读取当前 plan 状态
  校验 K 线收盘
  判断合法状态流转
  写 paper_events
  更新 paper_plans
  写 paper_snapshots
COMMIT
```

如果某个 plan 失败：

```text
ROLLBACK 当前 plan
记录 error
继续处理下一个 plan
```

要求：

1. 一个 plan 失败不影响其他 plan；
2. 不允许出现 event 写入成功但 plan 状态未更新；
3. 不允许出现 plan 状态更新成功但 event 未写入；
4. 事件写入、状态更新、snapshot 写入必须原子化。

---

# 八、K 线收盘确认机制

## 8.1 4h update 时间

未来 4h update 运行时间为：

```text
00:10
04:10
08:10
12:10
16:10
```

预留 10 分钟用于等待 4h K 线收盘和交易所数据同步。

---

## 8.2 严格 K 线收盘校验

在 paper update 中，对于依赖 4h K 线的逻辑，必须确认 K 线已经收盘。

要求：

1. API 返回字段如有 `is_closed`，必须检查 `is_closed = True`；
2. 如果没有 `is_closed` 字段，则必须通过 K 线 close_time 与当前 UTC 时间判断；
3. 当前时间必须大于 close_time，并建议额外预留安全缓冲，例如 1 到 3 分钟；
4. 未收盘 K 线不得用于：
   * reclaim 确认；
   * EMA 计算；
   * EMA trailing stop 抬高；
   * TP1 后 trailing 判断；
   * 任何需要已收盘 K 线确认的状态流转。

---

## 8.3 API_DELAY_SKIPPED 事件

如果目标 4h K 线未确认收盘，应：

1. 跳过该 symbol / plan 本轮状态更新；
2. 不修改 paper_plans；
3. 不写普通交易事件；
4. 写入 `API_DELAY_SKIPPED` 事件，或至少写日志；
5. 在 reason 中记录原因，例如：

```text
4h kline not closed at update time, skip state update
```

注意：

1. `API_DELAY_SKIPPED` 不应导致 plan 状态变化；
2. 不应重复大量写入同一 plan、同一 kline_time 的 API_DELAY_SKIPPED；
3. 可使用唯一约束或代码逻辑防重复。

---

# 九、daily_full 写入流程

daily_full 流程应为：

```text
start run: run_type = daily_full, status = running

scan
  -> 写 market_scans
  -> 写 scan_candidates

paper add-from-scan
  -> 根据 scan_candidates 创建 paper_plans
  -> 新建计划写 paper_events: PLAN_CREATED

paper update
  -> 读取 paper_plans
  -> 校验 K 线收盘
  -> 按单向状态机推进状态
  -> 状态变化写 paper_events
  -> 更新 paper_plans
  -> 写 paper_snapshots

paper report
  -> 从 SQLite 读取数据生成 md 报告

observation-dashboard
  -> 从 SQLite 读取数据生成 dashboard

finish run: status = success
```

失败时：

```text
finish run: status = failed
error_message = exception message
```

---

# 十、paper_4h_update 写入流程

paper_4h_update 流程应为：

```text
start run: run_type = paper_4h_update, status = running

paper update
  -> 读取已有 paper_plans
  -> 校验 4h K 线是否已收盘
  -> 按单向状态机推进状态
  -> 状态变化写 paper_events
  -> 更新 paper_plans
  -> 写 paper_snapshots

paper report
  -> 生成 4h update 轻量报告

observation-dashboard
  -> 生成 4h update dashboard 或更新观察数据

finish run: status = success
```

明确禁止：

```text
paper_4h_update 不执行 scan
paper_4h_update 不执行 paper add-from-scan
paper_4h_update 不创建新的交易计划
```

---

# 十一、报告生成要求

## 11.1 daily report

daily_full 继续生成：

```text
reports/YYYY-MM-DD/market_scan_*.md
reports/YYYY-MM-DD/paper_report_*.md
reports/YYYY-MM-DD/paper_observation_dashboard_*.md
```

要求：

1. 文件名带北京时间时间戳，避免覆盖；
2. 报告中标明 run_id；
3. 报告中标明 run_type = daily_full；
4. 报告中说明数据来自 SQLite；
5. 展示时间使用北京时间；
6. 如有必要，同时保留 UTC 时间字段。

---

## 11.2 4h update report

4h update 报告建议生成：

```text
reports/YYYY-MM-DD/paper_4h_update_HHMM_*.md
reports/YYYY-MM-DD/paper_4h_dashboard_HHMM_*.md
```

要求：

1. 文件名和标题明确包含 `paper_4h_update`；
2. 不得覆盖 daily_full 报告；
3. 报告中展示本次运行发生的状态变化；
4. 报告中展示当前开放计划；
5. 报告中展示 API_DELAY_SKIPPED 数量；
6. 报告中展示本次 run_id。

---

# 十二、db-summary 与复盘命令

新增基础查询命令：

```text
python main.py paper db-summary
python main.py paper db-events
python main.py paper db-export
```

至少支持以下统计：

1. 最近 N 次 run；
2. 失败 run 列表；
3. 当前开放计划；
4. 每个 plan 的事件路径；
5. RECLAIM_PENDING 总数及后续状态分类；
6. TP1_HIT 次数；
7. EMA trailing activated 次数；
8. EMA stop raised 次数；
9. EMA trailing stopped 次数；
10. CLOSED / STOPPED / EXPIRED / INVALIDATED 数量；
11. API_DELAY_SKIPPED 次数；
12. 每笔交易持仓小时数；
13. 每日 / 每 4 小时 snapshot 数量；
14. 导出 CSV。

建议导出路径：

```text
exports/paper_db_summary_YYYY-MM-DD.csv
exports/paper_events_YYYY-MM-DD.csv
exports/paper_snapshots_YYYY-MM-DD.csv
```

---

# 十三、Windows 计划任务：5 天后启用 4h update

## 13.1 启用原则

不要立即启用每 4 小时 paper update。

先保持当前每日 20:05 full daily，运行 5 天观察数据库与 daily 稳定性。

5 天内重点检查：

1. daily 任务是否每天成功；
2. runs 表是否连续；
3. LastTaskResult 是否为 0；
4. scan_candidates 是否正常写入；
5. paper_plans 是否没有重复创建；
6. paper_events 是否没有重复事件；
7. paper_snapshots 是否正常生成；
8. markdown 报告没有覆盖；
9. dashboard 与数据库统计是否一致；
10. 日志无明显异常；
11. SQLite 未出现 database is locked；
12. 所有时间戳均为 UTC；
13. 报告展示时间为北京时间。

---

## 13.2 新增脚本

5 天稳定后，新增：

```text
scripts/paper_4h_update.bat
scripts/install_4h_paper_task.ps1
```

`paper_4h_update.bat` 内容只允许包含：

```text
python main.py paper update --run-type paper_4h_update
python main.py paper report --run-type paper_4h_update
python main.py observation-dashboard --account demo --run-type paper_4h_update
```

或项目现有 CLI 的等价实现。

禁止包含：

```text
python main.py scan
python main.py paper add-from-scan
```

---

## 13.3 新增计划任务

任务名：

```text
CryptoTrading_4H_PaperUpdate
```

建议触发时间：

```text
00:10
04:10
08:10
12:10
16:10
```

不设置 20:10，因为 20:05 已有 daily_full，避免重复和潜在锁冲突。

日志路径：

```text
logs/paper_4h_update.log
```

每次运行日志至少包含：

```text
=== paper 4h update start ===
run_id: xxx
paper update done
paper report done
observation-dashboard done
=== paper 4h update complete ===
```

失败时记录：

```text
=== paper 4h update failed ===
run_id: xxx
error: xxx
```

---

# 十四、测试要求

## 14.1 数据库初始化测试

验证：

1. 数据库文件可创建；
2. 所有表可创建；
3. 所有索引可创建；
4. WAL 开启成功；
5. busy_timeout 设置成功；
6. foreign_keys 开启成功；
7. 重复初始化不报错；
8. schema version 可识别。

---

## 14.2 daily 写入测试

验证：

1. daily_full 能写入 runs；
2. scan 能写入 market_scans；
3. scan 能写入 scan_candidates；
4. add-from-scan 能写入 paper_plans；
5. 新计划写入 PLAN_CREATED；
6. paper update 能写入 paper_events；
7. paper update 能写入 paper_snapshots；
8. report 能正常生成；
9. dashboard 能正常生成；
10. run 成功后 status = success。

---

## 14.3 幂等性测试

验证：

1. 同一个 scan 重复导入不会重复创建 candidate；
2. 同一个 candidate 重复 add-from-scan 不会重复创建 plan；
3. 同一个 paper update 重复运行不会重复写 TP1、STOPPED、CLOSED；
4. 同一个 run 不会重复写 snapshot；
5. 失败后重跑不会导致状态错乱；
6. 终态不会回滚；
7. TP1_HIT 不会回滚到 ENTERED；
8. stop_current 不会被旧值降低。

---

## 14.4 状态机测试

至少构造以下状态流转测试：

```text
WATCHING -> RECLAIM_PENDING
RECLAIM_PENDING -> ENTERED
ENTERED -> TP1_HIT
TP1_HIT -> TP1_EMA_TRAILING_ACTIVATED
TP1_EMA_TRAILING_ACTIVATED -> TP1_EMA_TRAILING_ACTIVATED
TP1_EMA_TRAILING_ACTIVATED -> EMA_TRAILING_STOPPED
ENTERED -> STOPPED
WATCHING -> EXPIRED
```

同时验证非法流转被拒绝：

```text
CLOSED -> ENTERED
STOPPED -> ENTERED
TP1_HIT -> ENTERED
TP1_EMA_TRAILING_ACTIVATED -> ENTERED
EXPIRED -> WATCHING
INVALIDATED -> WATCHING
```

---

## 14.5 K 线收盘测试

验证：

1. 已收盘 4h K 线可用于 reclaim 和 EMA；
2. 未收盘 4h K 线不得用于状态推进；
3. 未收盘时写入或记录 API_DELAY_SKIPPED；
4. API_DELAY_SKIPPED 不改变 paper_plans.status；
5. API_DELAY_SKIPPED 不重复刷屏；
6. 交易所 API 延迟时任务不崩溃。

---

## 14.6 并发与锁测试

验证：

1. 人工执行 db-summary 时，不影响 daily 写入；
2. 快速连续执行两个 paper update，不产生 database locked；
3. 如果发生锁等待，最多等待 30 秒；
4. 超时后 run 标记为 failed；
5. 日志明确记录 database locked 发生在哪一步。

---

## 14.7 4h update 测试

验证：

1. paper_4h_update 不执行 scan；
2. paper_4h_update 不执行 add-from-scan；
3. paper_4h_update 不创建新计划；
4. paper_4h_update 只推进已有 paper_plans；
5. 4h update 能写 runs；
6. 4h update 能写 paper_events；
7. 4h update 能写 paper_snapshots；
8. 4h report 文件名与 daily report 明确区分；
9. 4h update 能正确处理 API_DELAY_SKIPPED；
10. 4h update 不与 20:05 daily_full 冲突。

---

# 十五、推荐开发顺序

## 第一阶段：数据库基础设施

1. 新增 SQLite 初始化模块；
2. 创建 schema；
3. 创建索引；
4. 设置 PRAGMA：
   * WAL；
   * foreign_keys；
   * busy_timeout；
   * synchronous=NORMAL；
5. 新增 db init / db status；
6. 编写数据库基础测试。

---

## 第二阶段：接入 daily_full

1. runs 接入 daily_full；
2. scan 写 market_scans；
3. scan 写 scan_candidates；
4. add-from-scan 写 paper_plans；
5. 新计划写 PLAN_CREATED；
6. paper update 写 paper_events；
7. paper update 写 paper_snapshots；
8. report / dashboard 从 SQLite 读取；
9. 保持 markdown 输出；
10. 确保失败时 runs.status = failed。

---

## 第三阶段：状态机与 K 线安全

1. 实现 Python 应用层状态机合法流转表；
2. 所有 UPDATE 带前置状态条件；
3. 每个 plan 独立事务；
4. 状态更新 + 事件写入 + snapshot 写入原子化；
5. 实现 K 线收盘校验；
6. 实现 API_DELAY_SKIPPED；
7. 补充状态机和 K 线测试。

---

## 第四阶段：连续 5 天观察

保持现有每日 20:05 daily_full，不启用 4h update。

每天检查：

```powershell
Get-ScheduledTaskInfo -TaskName CryptoTrading_DailyPaperUpdate |
    Select-Object LastRunTime, LastTaskResult, NextRunTime
```

并检查：

```text
runs 表有 daily_full success
reports/YYYY-MM-DD/market_scan_*.md 存在
reports/YYYY-MM-DD/paper_report_*.md 存在
reports/YYYY-MM-DD/paper_observation_dashboard_*.md 存在
logs/daily_paper_update.log 正常结束
无 database is locked
无重复 plan
无重复 event
无状态回滚
```

---

## 第五阶段：新增 4h paper update

5 天稳定后，新增：

```text
scripts/paper_4h_update.bat
scripts/install_4h_paper_task.ps1
```

新增任务：

```text
CryptoTrading_4H_PaperUpdate
```

运行时间：

```text
00:10
04:10
08:10
12:10
16:10
```

该任务只运行：

```text
paper update
paper report
observation-dashboard
```

不运行：

```text
scan
add-from-scan
```

---

## 第六阶段：复盘支持

新增：

```text
python main.py paper db-summary
python main.py paper db-events
python main.py paper db-export
```

三周结束后应能回答：

1. 三周内一共有多少次 scan；
2. 一共有多少个 BUY_CANDIDATE；
3. 一共有多少个 paper plan；
4. RECLAIM_PENDING 出现多少次；
5. RECLAIM_PENDING 后续有多少重新入场；
6. 有多少 pending 后跌破 stop 或失效；
7. TP1 触发多少次；
8. EMA trailing 激活多少次；
9. EMA stop 抬高多少次；
10. EMA trailing 导致退出多少次；
11. API_DELAY_SKIPPED 出现多少次；
12. 每笔交易持仓时间；
13. 每笔交易最终状态；
14. 4h update 是否提升了事件捕捉精度；
15. daily_full 与 paper_4h_update 是否一致、连续、无重复。

---

# 十六、最终验收标准

本次开发完成后，应满足：

1. daily_full 每天正常写入 SQLite；
2. markdown 报告仍正常生成；
3. SQLite 成为 paper trading 主数据源；
4. 不依赖解析 markdown 才能复盘；
5. 重复运行不会产生重复计划和重复事件；
6. 所有状态变化都有 paper_events 记录；
7. 每次 paper update 后都有 paper_snapshots；
8. 数据库启用 WAL；
9. SQLite 连接设置 timeout；
10. 所有数据库时间统一为 UTC；
11. Markdown / dashboard 展示北京时间；
12. 4h K 线未收盘时不推进状态；
13. API 延迟时写 API_DELAY_SKIPPED 或日志；
14. 状态机只能单向推进，不允许回滚；
15. 每个 plan 的状态推进具备事务原子性；
16. 查询性能有基础索引保障；
17. 5 天稳定后可安全启用 4h paper update；
18. 4h update 不改变选币频率，不新增计划，只更新已有计划和持仓；
19. 三周后可以直接用数据库统计模拟盘观察结果。
