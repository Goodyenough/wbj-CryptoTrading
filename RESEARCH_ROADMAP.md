# CryptoTradingSystem 研究路线图

更新时间：2026-07-26 18:00 +08:00

## 1. 当前阶段判断

当前项目应暂停“继续优化参数”，进入：

> 阶段 1 到阶段 2 之间的审计与理解整理期。

原因：

- 基础设施已经很多：扫描、回测、动态 universe、A/B、模拟盘、paper audit、shadow replay、SQLite 运行审计都已存在。
- 策略规则也已经较多：regime、entry reclaim、EMA trailing、max holding、relative strength、ATR reclaim。
- 现在最大风险不再是“缺一个指标”，而是“每个指标单独合理，但整体优势来源无法清楚解释”。

本阶段的目标是把系统重新变成项目所有者可以解释的研究系统。

## 2. 五阶段全局路线

| 阶段 | 核心问题 | 当前状态 | 下一步 |
|---|---|---|---|
| 阶段 1：基础设施可信 | 数据和程序可信吗？ | 部分完成 | 继续审计未来数据、回测/模拟盘一致性、任务稳定性 |
| 阶段 2：稳定优势 | 策略是真有优势，还是某段历史碰巧有效？ | 进行中 | 停止叠加规则，先复核现有实验独立性 |
| 阶段 3：优势来源 | 策略为什么赚钱？ | 不充分 | 做模块归因，而不是继续参数搜索 |
| 阶段 4：模拟盘验证 | 回测系统能否可靠转成实际运行？ | 进行中 | 继续观察信号、漏信号、延迟、右截尾、机会成本 |
| 阶段 5：小资金实盘 | 是否能用小资金验证执行？ | 未开始 | 暂不进入 |

## 3. 已解决的问题

| 问题 | 证据 |
|---|---|
| 能从 Binance 扫描 USDT 现货市场并输出候选 | `python main.py scan`、scan reports |
| 能生成可人工复核的交易计划 | 报告包含 entry、stop、TP1、TP2、风险提示、图表链接 |
| 能把 scan 候选导入模拟盘 | `paper add-from-scan` 默认只导入 `BUY_CANDIDATE` |
| 模拟盘有结构化状态 | `paper_plans`、`paper_events`、`paper_snapshots` |
| 回测与模拟盘共用状态推进核心 | `trade_state.py` |
| 能做动态 universe 回测 | `backtest-dynamic-universe` |
| 能做单变量 A/B | `abtest.py` override 白名单 |
| 能汇总跨窗口实验 | `abtest-summary`、`experiment-index` |
| 能检查样本不足和窗口重叠 | summary 报告包含 sample / overlap 规则 |
| 能做模拟盘离线 shadow 归因 | `paper shadow-replay`、`paper shadow-experiment` |

## 4. 尚未解决的问题

| 问题 | 为什么重要 | 当前处理 |
|---|---|---|
| 策略优势来源未完全归因 | 不知道收益来自选币、入场、regime、exit 还是少数极端行情 | 本次新增系统总览和实验账本 |
| 历史退市币缺失 | dynamic universe 仍依赖当前 Binance master，可能幸存者偏差 | 后续需要历史 symbol master |
| intrabar 真实路径不清 | 4h K 线内 stop/TP 顺序只能假设 | 当前用 `stop_first` 保守处理，后续可测 15m/5m |
| 资金占用和机会成本未充分计量 | max holding / longer wait 可能提高收益但占用资金 | 需要加入 exposure、holding time、median return 等解释 |
| 规则组合越来越复杂 | 容易把上一次改动的问题用下一次改动修补 | 后续必须先写实验卡片 |
| 模拟盘样本仍有右截尾 | 尚未成熟的机会不能作为最终判断 | checkpoint 已标记 right-censored |

## 5. 暂停新增实验期间的工作顺序

1. 阅读 `SYSTEM_OVERVIEW.md`，确认你能用自己的话解释系统各模块。
2. 阅读 `EXPERIMENT_LEDGER.md`，把每个实验理解成“回答了哪个问题”，而不是只看收益。
3. 阅读本 `RESEARCH_ROADMAP.md`，确认项目当前不应直接进入实盘，也不应继续叠加新规则。
4. 抽查 3 到 5 个关键报告，确认账本没有把 `retest` 写成 `keep`。
5. 只在上述内容能被项目所有者解释清楚后，再审批下一张实验卡片。

## 6. 下一步候选研究，不是立即执行

这些是候选方向，不是当前授权执行的实验。

| 优先级 | 候选方向 | 为什么排在这里 | 执行前条件 |
|---|---|---|---|
| P0 | 实验卡片制度落地 | 防止继续黑箱化 | 用户确认新流程 |
| P1 | `atr_reclaim` 阈值敏感性 `0.10/0.15/0.35` | 当前 `0.25` 改善收益但早期 MDD 恶化 | 先写实验卡片，只改同一维度 |
| P1 | `max_holding_42` 模拟盘复核 | 固定 42 根候选强，但不能只靠回测部署 | 收集真实 TIME_EXIT 或长持仓样本 |
| P2 | 历史 symbol master | 降低幸存者偏差 | 找到可靠历史交易对来源 |
| P2 | intrabar 15m/5m 裁决 | 检查 4h `stop_first` 是否过保守或失真 | 成本和数据缓存可接受 |
| P2 | 模块归因实验 | 判断收益来自哪里 | 需要固定 baseline 和统一窗口 |

## 7. 以后每次实验的审批规则

在提出任何新实验、参数调整或策略修改之前，必须：

1. 说明当前系统的全局目标。
2. 说明本次实验要回答的唯一问题。
3. 说明该问题在整体研究路线中的位置。
4. 区分事实、观察、假设和决策。
5. 给出支持、否定及证据不足的判定标准。
6. 禁止仅因为回测收益提高就推荐修改默认参数。
7. 实验完成后必须用“非技术解释 + 技术证据”两种方式解释。
8. 除非用户明确批准，不得修改生产配置。
9. 每次实验结束后更新实验账本和当前策略说明书。
10. 当用户无法解释某项修改时，优先帮助用户理解，不继续增加复杂度。

## 8. 项目所有者自检问题

关闭 GPT/Codex 对话后，如果不能回答下面四个问题，就不应进入下一轮优化：

- 当前策略为什么可能赚钱？
- 当前最可信的规则是哪几条，证据是什么？
- 当前哪些规则只是候选，不允许部署？
- 下一步实验为什么排在其他实验前面？

当前答案摘要：

- 可能赚钱的来源：筛掉明显低质量标的，在较好大盘环境中等待趋势恢复确认，再用止损和 EMA trailing 控制错误与利润回撤。
- 较可信规则：数据质量过滤、RISK_OFF 暂停核心币新开仓、entry reclaim、TP1 EMA trailing。
- 候选但未部署：relative strength soft gate、ATR reclaim margin、max holding 42。
- 下一步不应马上实验；应先读懂系统地图和实验账本。若恢复实验，优先同维度复核 `atr_reclaim` 阈值，而不是叠加新过滤器。

