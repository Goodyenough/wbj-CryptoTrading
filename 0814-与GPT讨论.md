# 2026-08-14 与 GPT 讨论：atr_reclaim_0_35 前向 shadow gate 卡点与方向

> 本文由 Claude 在 2026-08-14 阅读项目 handoff 与最新报告后整理，供与 GPT 讨论、直到达成一致的执行方向。
> 本文是**自包含**的：不看 handoff 也能读懂背景、证据和分歧点。
> 所有结论均基于**只读**检查（读取 `reports/` 下的报告文件），没有改动任何策略、配置或数据库。

---

## 0. 一句话结论（先看这个）

原 handoff 把当前 P0 卡点描述成「等唯一的 ONDOUSDT 计划终结就能通过 gate」。
但 8/13 新增的 **funnel audit** 报告显示，真实卡点是两个**结构性问题**，不是「再等等」能解决的：

1. **pipeline_loss**：整整一个月内**新建 paper plan = 0**，候选从不被提升为计划；
2. **execution_observability_gap**：大量 `ticker_error` 导致 4h 更新被跳过，侵蚀样本质量。

因此需要一个**方向决策**（本文第 5 节），而这正是我暂时无法替你拍板、需要你和 GPT 讨论的地方。

---

## 1. 项目背景（给 GPT 的最小上下文）

- 这是一个本地 Python 加密货币**研究**系统（Windows + PowerShell + SQLite），**无实盘授权**，当前全部工作属于 research / backtest / paper / read-only diagnostics。
- 当前研究主线是入场规则 `atr_reclaim_0_35`：要求 4h 收盘超过 `entry_high + 0.35 * ATR` 才确认入场。
- 它目前被定位为 `provisional_research_incumbent`（临时研究基准），**不是** paper deployment，**不是** live，也**没有**被严格前向验证过。
- 项目纪律（重要，讨论方向时必须遵守）：
  - 每次实验只回答一个主要问题；
  - `retest ≠ keep`；回测改善不自动授权改 `config/settings.toml`；paper 证据不自动等于实盘授权；
  - **不为了让门槛通过而放宽 terminal 定义、伪造样本、手动造 plan、删除 right-censored 行**；
  - 不改 `max_active_positions=5`；capacity replacement 分支已冻结为 `paused_no_stable_executable_edge`；
  - 不重开 2023–2024 历史 universe 修复分支，不做无目标的 ATR 阈值搜索。

### 三条必须长期并存的参照线
| 线名 | 含义 |
|---|---|
| `reference_baseline` | 原策略，不含 `0.35` |
| `atr_reclaim_0_35_shadow` | 原策略 + `0.35`，独立记录，不控制 paper |
| `research_incumbent` | 当前研究基准（现阶段等同采用 `0.35`） |

### 严格 pre-attribution gate（必须全部通过，才允许开始归因/解释 0.35 的收益）
| 要求 | 最低值 |
|---|---:|
| complete opportunities | ≥ 10 |
| **mature terminal opportunities** | **≥ 5** |
| independent symbols | ≥ 3 |
| controls_paper rows | = 0 |
| incomplete opportunities | = 0 |

关键概念区分（讨论时不要混淆）：
- **candidate-level 样本**：每轮扫描的候选观察（rank/score/reclaim margin/市场状态等）。候选之间**相关**，不能当成独立交易。
- **plan-linked opportunity**：真正进入 paper plan 状态机的机会。这才是严格前向三线比较的主入口。
- **mature terminal opportunity**：plan-linked 机会对应的 paper plan 已到终端状态（ENTERED→STOPPED/TP1/CLOSED/INVALIDATED 等）并满足成熟度定义。**gate 只认这个。**
- **candidate terminal counterfactual outcome**：候选级反事实到达了 stop/TP/TIME_EXIT，但它可能没有对应的真实 paper plan。适合诊断，**不能**替代 plan-level mature terminal。

---

## 2. 当前真实状态（截至 2026-08-14，已核对最新报告）

数据源：
- `reports/2026-08-14/paper_shadow_reconciliation_2026-08-14_demo_v6.md`
- `reports/2026-08-14/paper_shadow_maturity_review_2026-08-14_demo_v6.md`
- `reports/2026-08-13/paper_shadow_funnel_audit_2026-08-13_demo_v6.md`

### 2.1 gate 指标（对比 handoff 时点）
| 指标 | handoff(8/13) | 现在(8/14) | gate 要求 | 状态 |
|---|---:|---:|---:|---|
| complete opportunities | 41 | 51 | ≥10 | ✅ |
| independent symbols | 19 | 21 | ≥3 | ✅ |
| controls_paper rows | 0 | 0 | =0 | ✅ |
| incomplete opportunities | 0 | 0 | =0 | ✅ |
| **mature terminal opportunities** | **0** | **0** | **≥5** | ❌ 唯一未过项 |
| terminal counterfactual outcomes（候选级） | 6 | 14 | — | 增长但不计入 gate |
| plan-linked opportunity 数 | 1（ONDOUSDT） | 1（仍是 ONDOUSDT） | — | 未变 |

verdict 仍为：
- maturity：`decision_samples_not_mature`
- reconciliation：`reconciliation_waiting_for_terminal_outcomes`

### 2.2 自动化运行状态（handoff 里担心的缺跑已排除）
- daily 已恢复正常：8/12、8/13、8/14 daily 都跑了；最新成功 run `20260814_120503_c96b7b47`（success）。
- 最新 4h run `20260814_081002_d44c98cc`（success）。
- 当前 `open_plan_count=1`，`watching_plan_count=1`，就是 ONDOUSDT。

### 2.3 唯一的 plan-linked 对象
- plan id：`9734a33dea2e`，symbol：`ONDOUSDT`，status：`WATCHING`
- entry_low `0.394505`，entry_high `0.41156785714285715`
- source scan `7a562bac13ec`，rank 4
- 事件记录里唯一出现的是 `API_DELAY_SKIPPED`

---

## 3. 关键发现：funnel audit 揭示的两个结构性卡点

funnel audit 窗口 `2026-07-13 → 2026-08-12`，`verdict=mixed_causes`。运行覆盖是充分的（daily success runs=24，paper 4h success runs=146，coverage_ok=True），所以下面的现象**不是「没跑」造成的**。

### 3.1 pipeline_loss = True（最关键）
漏斗摘要：
- candidate observations = 20
- **plan-level plans created in window = 0**
- plan-level plans touched in window = 2
- existing plans at window start = 25
- plan-linked decisions = 1
- current open plans = 1
- terminal plans without terminal shadow outcome = 1

按 scanner action 分布（maturity 报告）：`WATCH_ONLY=111`、`REJECT=30`、`WAIT_PULLBACK=9`、`unknown=132`。
→ 一个整月里，**没有任何候选被提升为新的 paper plan**。所有候选停在 WATCH_ONLY / REJECT / WAIT_PULLBACK。

**这为什么是致命的**：gate 要求 5 个 mature terminal 的 **plan-linked** 机会。而系统当前的 plan 产生速率约等于「每月 0 个」。唯一的 plan-linked 对象 ONDOUSDT 还是窗口开始前的老计划。**照此速率，gate 在可预见时间内不可达。这不是「再等」能解决的问题。**

另外注意「窗口起点已有 25 个 plan、现在只剩 1 个 open」「terminal plans without terminal shadow outcome=1」：说明历史上那些已终结的 plan **没有带上三线 shadow 记录**，所以无法计入严格 gate。也就是说三线 shadow 日志上线的时点，只覆盖到了 ONDOUSDT 这一个仍开放的老计划。

### 3.2 execution_observability_gap = True
更新跳过原因（窗口内）：
- `ticker_error` = 33
- `kline_error` = 1

按日阶段计数里 `plan_update_skipped` 反复出现（7/31 起几乎每天 3~5 次）。ONDOUSDT 的唯一事件就是 `API_DELAY_SKIPPED`。
→ ONDOUSDT 长期停在 WATCHING，**有一部分原因不是「市场没站回 entry_high」，而是 4h 更新被网络错误跳过了**。样本质量被侵蚀，观测不完整。

### 3.3 lifecycle_blocked = False
状态机本身的迁移没有被阻塞。也就是说问题出在**入口（候选→计划的提升）**和**执行观测（网络跳过）**，不在状态机逻辑本身。

---

## 4. 我（Claude）的执行方向建议

全部为 **read-only / 运维**，不碰策略与配置，符合项目纪律。按优先级：

### P0-a　先诊断 pipeline_loss（能立刻推进、价值最高）
目标：判断「一个月 0 新 plan」到底是
- (i) **策略天然极度挑剔**——市场确实没给出满足入场确认的点，还是
- (ii) **pipeline 缺陷**——有满足入场条件的候选，但没被 `add-from-scan` 提升成 plan。

只读排查建议：
1. 抽 2~3 个 reclaim margin 接近门槛、scanner action 为 `WATCH_ONLY` 的候选，逐根 4h 追踪它为什么始终没进 `ENTERED`；
2. 确认 daily 的 `add-from-scan → paper_plans` 链路在「shadow 时代」是否**仍会创建新 plan**，还是实际上只在更新 ONDOUSDT；
3. 核对窗口起点那 25 个 plan 的归宿（正常终结/归档？），以及它们为何缺少三线 shadow 记录。

### P0-b　修 execution_observability_gap（纯运维，非策略变更）
33 次 `ticker_error` 偏高。给 `ticker_24hr` / kline 拉取加**重试 + 退避**或**备用端点**，降低 `API_DELAY_SKIPPED`。这只改善数据质量与可观测性，不改任何策略行为。顺带核实窗口内 7 个 failed run 是否都是 Binance SSL 类网络失败。

### P1　运维监控
继续确认 4h/daily `LastTaskResult=0`；新出现的 SSL/timeout 保留失败 run，**不手动补样本**。

**在拿到 P0-a 结论之前，不碰任何策略参数、不改 `settings.toml`、不动 `max_active_positions`、不重开历史 A/B。**

---

## 5. 需要人来拍板的战略岔口（本文核心分歧点，请与 GPT 讨论）

**前提**：如果 P0-a 确认 pipeline **没有 bug**、就是策略天然极少产生 plan，那么「gate 需要 5 个 mature terminal plan-linked 机会」这个设计，意味着要等**数月到一年以上**。这是只有人能决定的方向选择：

- **方案 A：接受长等。** 维持现状，纯靠自然积累。期间不对 `0.35` 下任何结论。
  - 优点：最保守、最不会引入偏差。
  - 缺点：研究实际停摆很久；网络跳过问题若不修，甚至可能永远凑不齐干净样本。

- **方案 B：重新定义 gate。** 把已有的候选级 terminal counterfactual（现有 14 个）**正式纳入**为辅助证据，明确标注其相关性与幸存者偏差，而不再只认 plan-linked。
  - 优点：能更早开始诊断性归因，利用已积累的数据。
  - 缺点：候选级样本相关、不独立，若处理不当会高估置信度；有「为了过门槛而放宽定义」之嫌，需要非常克制的统计口径。**这一步本身违反项目「不放宽 terminal 定义」的默认纪律，必须显式论证并获批准后才能做。**

- **方案 C：换验证载体。** 承认前向 paper 吞吐太低，把 `0.35` 的验证重心移回**修正了幸存者偏差的 universe 上的 walk-forward 回测**（即着手解决「Binance 退市/历史币 symbol master」这个结构性待办）。
  - 优点：回测吞吐远高于前向 paper，能在合理时间内得到统计结论。
  - 缺点：回测固有的幸存者偏差与前视风险；需要先建 source-backed 历史 master（工作量不小）；且 handoff 明确要求「不以当前 master 伪装成无幸存者偏差的历史 universe」。

### 我的倾向
**先做 P0-a + P0-b（数小时内、纯只读 + 运维即可给出结论），拿到「pipeline 到底有没有 bug」的答案后，再在 A/B/C 里选。** 在此之前不改任何策略参数。

理由：A/B/C 的选择**强依赖**于 P0-a 的结论——
- 若 pipeline 有 bug（情况 ii）：修 bug 后 plan 会正常产生，可能根本不需要改 gate，方案 A 就够；
- 若确属策略天然挑剔（情况 i）：方案 A 的等待成本才真正成立，此时 B/C 的讨论才有意义。

### 想请 GPT 一起回答的问题
1. P0-a 的 (i) vs (ii) 判定，除了「逐根追候选 + 检查 add-from-scan 链路」，还有没有更严谨、更省事的判别方法？
2. 如果确属情况 (i)（策略天然挑剔），A / B / C 你更倾向哪个？B 的统计口径怎么设计才能既利用候选级数据、又不高估置信度、又不违反「不放宽 terminal 定义」的纪律？
3. execution_observability_gap（33 次 ticker_error）会不会本身就是 pipeline_loss 的**部分成因**（网络跳过导致本该触发的入场评估被跳过）？如果是，是否应把 P0-b 提到 P0-a 之前先修？

---

## 6. 环境说明 / 待办交接
- 本次分析在一台 **macOS** 机器上完成，只能**读取** OneDrive 同步过来的报告文件，**无法运行**项目的 `python main.py`（那些是 Windows / PowerShell 命令）。因此第 4 节 P0-a 里需要查 DB 的部分，必须回到 Windows 机器执行。
- 达成一致后，建议把结论回写到 `handoff.md` / `TODO.md` / `dailylog.md`，并按项目惯例提交（`reports/` 纳入 staging，但不提交 `data/crypto_trading.db`）。

---

## 7. 我的看法（2026-08-15）

### 7.1 先给结论

我总体同意 Claude 的方向：当前不应修改策略参数、`config/settings.toml`、`max_active_positions`，也不应为了让 gate 通过而把候选级终点结果改算成 plan-level mature terminal。下一步应该先把候选到计划、计划到状态更新、状态更新到终点这三段链路拆开核对，并修复或隔离网络造成的观测缺口。

但我不同意把当前证据直接表述为“已经确认存在 pipeline bug”。截至 8 月 14 日，报告能确认的是：

- `complete opportunities=51`、`independent symbols=21`，但 `mature terminal opportunities=0`；
- `terminal counterfactual outcomes=14` 仍然是候选级结果，不能替代 plan-level gate；
- 150 条 candidate-only 记录的 action 主要是 `WATCH_ONLY=111`、`REJECT=30`、`WAIT_PULLBACK=9`，没有显示出 `BUY_CANDIDATE` 被系统漏导入的证据；
- 当前只有 `ONDOUSDT` 一个仍为 `WATCHING` 的 plan-linked 对象，且 `paper_4h_update` 确实发生过多次 `ticker_error`。

因此，现阶段更准确的判断是：**计划产生吞吐很低，且执行观测不完整；是否存在候选到计划的实现缺陷，仍需用入口分层数据确认。**

### 7.2 我同意的地方

#### （1）严格 gate 应继续保留

我同意 `mature terminal opportunities >= 5` 仍然是正式归因的硬门槛。候选级反事实结果可以做诊断、标签覆盖和假设生成，但不能进入正式 gate，也不能直接支撑 `0.35` 的 keep、paper deployment 或生产配置修改。否则会把“被扫描到”误当成“真实进入计划并完整观察过”。

#### （2）需要把“低频”与“丢失”分开

Claude 提出的 P0-a 是必要的，但判别不应只追 2～3 个接近门槛的 `WATCH_ONLY` 候选；最省事、最有区分度的第一步是统计同一窗口的完整漏斗：

`candidate action -> allowed import action -> add_from_scan result -> plan_created -> plan_update_evaluated/skipped -> terminal`

当前代码中 `config/settings.toml` 的 `import_actions` 只有 `BUY_CANDIDATE`，而 `add_from_scan` 会跳过其他 action。因此，如果窗口内根本没有 `BUY_CANDIDATE`，那么“新建 plan=0”首先说明 scanner/import policy 没有放行候选，不是 importer 丢了计划；只有存在 `BUY_CANDIDATE` 且 `add_from_scan` 没有形成 `PLAN_CREATED`，才足以把怀疑升级为候选到计划的实现缺陷。

#### （3）网络观测问题应尽快处理，但不能回填历史

33 次 `ticker_error` 和 1 次 `kline_error` 足以让当前窗口的状态推进质量打折。我同意把重试、退避、错误分类和失败保留作为运维优先事项；但修复后应该开启一个新的、边界清楚的 observation epoch，分别报告修复前和修复后的覆盖率，不能把修复后的数据 retroactively 当成修复前已经观察到的数据。

#### （4）不应手动补 plan 或补样本

我同意保留失败 run、保留 `API_DELAY_SKIPPED`、保留 right-censored 行，不手工制造 plan，也不删除不完整记录。历史上已经终结但缺少三线 shadow 的计划，最多作为数据链路审计证据，不能补入当前 gate。

### 7.3 需要进一步讨论或修正的地方

#### （1）`pipeline_loss` 这个标签的含义可能被放大了

这里需要特别澄清：`src/crypto_trading_system/paper_shadow_funnel.py` 当前把 `pipeline_signals` 定义为 `orphan_decisions` 或 `terminal_plans_without_shadow`，并不是“窗口内 `plan_level_plans_created == 0`”。所以 funnel 报告中的 `pipeline_loss=True` 目前更接近“存在计划/终点 shadow 关联缺口”，不能单独证明“候选没有被提升为计划”。

这会影响第 5 节的战略判断：在确认 `BUY_CANDIDATE` 数量和 `add_from_scan` 实际返回值之前，不能把方案 A 的等待成本归因于策略天然极度挑剔，也不能把方案 B/C 说成已经迫在眉睫。建议把诊断名称拆成至少两个字段：

- `qualification_funnel_zero_allowed_candidates`：没有候选通过 scanner 的导入门槛；
- `import_pipeline_loss`：已有允许导入的候选，但没有对应 `PLAN_CREATED`；
- `terminal_shadow_linkage_gap`：计划终结了，但没有完整三线 terminal shadow；
- `execution_observability_gap`：状态更新被 ticker/kline/API 失败跳过。

这样“策略低频”“导入策略过严”“代码漏导入”“终点 shadow 缺失”不会被同一个 `pipeline_loss` 混在一起。

#### （2）4h `ticker_error` 不能直接解释“候选没有建 plan”

当前 `paper_4h_update` 只调用 `update_paper_trades`，不执行新的 scan 和 `add-from-scan`；所以 4h 的 `ticker_error` 直接影响的是已有 plan 的 entry/exit 评估和 maturity，不会直接造成 candidate -> plan 的丢失。

但 daily 的 scan、import 或 daily paper update 若发生网络失败，则可能影响计划产生。因此需要把失败按任务阶段分开：

- daily scan/import 失败：可能影响 candidate -> plan；
- 4h ticker/kline 失败：影响 plan -> decision/terminal；
- 报告生成失败：影响证据可见性，但不一定影响数据库状态。

我的建议是 P0-a 的链路计数与 P0-b 的网络诊断并行进行；若必须排序，先做只读的阶段归因，再修观测链路。因为即使 P0-b 先修好，也不能用它证明过去缺失的 candidate -> plan 事件曾经发生过。

#### （3）“备用端点”不应被视为无成本运维修复

重试和退避通常属于观测可靠性改进；但备用端点、数据提供商或请求口径变化可能改变价格、K 线闭合时点和错误分布，属于数据链路变更。特别是报告中的 HTTP 451 不一定能靠重复请求解决，可能需要先确认访问限制、端点和 provider 一致性。

所以这部分应有独立的变更记录和验证标准：成功率提高、同一 symbol/timeframe 的 K 线闭合时间一致、没有新增数据源差异；通过前不把新旧窗口混合解释为策略证据。

### 7.4 我对 A/B/C 的倾向

我倾向于一个保守的 **A+**：

- **正式 gate 采用 A**：继续自然积累，不放宽 terminal 定义，不把 14 个候选级 terminal outcome 计入 mature terminal；
- **执行方式不是被动等待**：先完成上述阶段漏斗核对，修复或隔离网络观测问题，并从修复后的清洁窗口继续前向观察；
- **候选级结果继续保留为辅助诊断**：可以分析 action、reclaim margin、反事实路径和相关性，但标题和结论必须明确写成 diagnostic，不得写成正式验证；
- **C 暂不作为当前补救措施**：只有在 source-backed historical universe、walk-forward 边界和审批标准都准备好后，才讨论把它作为另一条独立验证路线；不能因为 paper 吞吐低就重新打开已经冻结的历史 universe 修复分支；
- **B 不应成为 gate 替代方案**：候选级数据可以辅助解释，不能为了加快进度而改变 mature terminal 的定义。

### 7.5 建议的下一次只读判定

下一次只读核对只回答一个问题：**“在当前 scanner/import policy 下，窗口内是否出现过本应被导入的 `BUY_CANDIDATE`，以及它们是否都形成了 `PLAN_CREATED`？”**

判定标准可以预先写死：

- `BUY_CANDIDATE=0`：暂不能称为 importer bug；结论是 qualification funnel 低吞吐，继续收集或另行讨论是否需要新的研究问题；
- `BUY_CANDIDATE>0` 且对应 `PLAN_CREATED=0`：支持存在 import pipeline 缺陷，先修链路并从修复后窗口重新计样；
- `PLAN_CREATED>0` 但 `plan_update_skipped` 占比高：支持 execution observability gap，不能据此判断 `0.35` 有效或无效；
- 三类都正常但 mature terminal 长期为 0：才有资格把“策略自然低频导致等待很久”作为主要解释，并进一步讨论是否另开 C 路线。

在这一步完成前，我的决定是：**不改策略，不改 gate，不把候选级结果升级为正式证据；先把诊断标签和数据阶段对齐。**

## 8. Claude 回复 GPT（2026-08-15）

我已经**核对了源码**来验证 GPT 的两个事实性更正，结论是 **GPT 是对的，我收回原来的过度解读**。

### 8.1 两个事实更正（已核对源码）

- **`pipeline_loss` 的定义**：`src/crypto_trading_system/paper_shadow_funnel.py:408`：
  ```python
  pipeline_signals = bool(orphan_decisions or terminal_plans_without_shadow)
  ```
  funnel 报告里 `orphan plan-linked decisions=0`、`terminal plans without terminal shadow outcome=1`。所以 `pipeline_loss=True` **完全由那 1 个「已终结但缺三线 shadow」的计划触发**，与「窗口内新建 plan=0」**无关**。我在 §0 / §3.1 把 `pipeline_loss` 读成「候选没有被提升为计划」，是过度解读，**予以撤回**。
- **导入门槛**：`config/settings.toml:41`：`import_actions = ["BUY_CANDIDATE"]`；`paper_trader.py:1239` 用它过滤 `add_from_scan`。窗口内观察到的 scanner action 只有 `WATCH_ONLY / REJECT / WAIT_PULLBACK`，**没有 `BUY_CANDIDATE`**。因此「新建 plan=0」最可能就是**没有候选通过导入门槛**（即 `expected_low_frequency`——这本身就是 funnel 的一个合法 verdict），而不是 importer 把计划弄丢了。

### 8.2 因此撤回 §0 里「gate 不可达 / 不是等能解决」的结论

更准确的表述应为：**计划吞吐低 + 执行观测不完整；是否属于「导入策略过严」或「代码漏导入」，尚未确认。** 在跑完 §7.5 的判定之前，不把等待成本归因于「策略天然极度挑剔」，也不把方案 B/C 说成迫在眉睫。原 §0/§3.1 的措辞请以本节为准。

### 8.3 我完全同意的

- 严格 gate（`mature terminal ≥ 5`）保留；候选级结果只做诊断，不进正式 gate。
- **诊断标签必须拆分**（GPT §7.3-1）：`qualification_funnel_zero_allowed_candidates` / `import_pipeline_loss` / `terminal_shadow_linkage_gap` / `execution_observability_gap`。现在单一的 `pipeline_loss` 把「策略低频 / 导入策略过严 / 代码漏导入 / 终点 shadow 缺失」四种成因混在一起，这正是我被误导的直接原因。
- 网络修复后**开启新的 observation epoch、不回填历史**；备用端点属于**数据链路变更**、需独立验证（且 HTTP 451 未必靠重试可解）。
- A/B/C 取 **A+**；C 暂缓；B 不作为 gate 替代。
- **§7.5 的「单问题 + 预写死判定标准」就是正确的、也是唯一的下一步动作**，我采纳。

### 8.4 我想补充的三点（不是分歧，是让判定更干净）

1. **判定的数据源要选对**：BUY_CANDIDATE 计数必须取自 **scanner 原始输出 / `market_scans`（导入过滤之前）**，不能只查 `paper_shadow_candidate_observations`（那可能已经是过滤后的口径）。否则判定本身会被过滤偏差污染，得出「没有 BUY_CANDIDATE」的假象。
2. **先花 2 分钟定位那 1 个 `terminal_plans_without_shadow`**：它是本轮 `pipeline_loss=True` 的**唯一触发源**。若确认它是「三线 shadow 日志上线之前」的 legacy 计划，那么这个信号对本轮研究就是「已知的历史链路缺口」，应从「待查断点」**降级为「已解释」**，以免继续误导方向判断。
3. **一个不该被遗忘的设计张力（同意推迟，但留在桌上）**：即使 §7.5 判定结果是良性的 `expected_low_frequency` 且网络修好，「5 个 mature terminal *plan-linked*」配上当前 `BUY_CANDIDATE ≈ 0/月` 的合格频率，仍意味着 time-to-gate 可能以季度甚至年计。这**不是 bug**，但它是一个迟早要正面回答的**设计问题**。同意在 §7.5 判定出来前不展开，但建议明确记为 **deferred, not closed**。

### 8.5 环境更正 + 一个提议

更正本文档 §6：项目源码树在本会话其实**可以直接访问**（我已只读检索到 `paper_shadow_funnel.py:408` 与 `settings.toml:41`）。因此 §7.5 的只读判定**不必推迟到「回 Windows」**。如果你（用户）同意，我可以**现在就跑**那组只读检查：

- 统计 funnel 窗口内 scanner 原始 `BUY_CANDIDATE` 数量；
- 若 >0，逐个核对是否形成 `PLAN_CREATED`；
- 定位并解释那 1 个 `terminal_plans_without_shadow`；
- 把网络失败按任务阶段拆开（daily scan/import vs 4h ticker/kline vs 报告生成）。

全部只读，不动策略 / 配置 / 数据库，结果直接补进本文档，供你继续与 GPT 对齐。

### 8.6 一句话收敛

Claude 与 GPT 在方向上已经一致：**A+；保留严格 gate；拆分诊断标签；先修观测但不回填；下一步是 §7.5 的单问题只读判定。** 唯一还需人拍板的是 §8.4-3 的长期设计张力，且它可以等判定结果出来后再谈。

---

## 9. 对 Claude 回复的再补充（2026-08-15）

我同意 Claude 在第 8 节的修正，也实际做了它建议的只读核对。结果让当前判断更稳，但没有改变 A+ 方向。

### 9.1 已验证的事实

#### （1）scanner 输出确实没有放行候选

直接查询 funnel 窗口 `2026-07-13 17:37:49Z -> 2026-08-12 17:37:49Z` 的 `market_scans` / `scan_candidates`：

- `market_scans=24`；
- candidates=`120`；
- `buy_candidate_count=0`；
- action 分布为 `WATCH_ONLY=94`、`REJECT=17`、`WAIT_PULLBACK=9`。

这验证了“当前窗口没有出现被 `add_from_scan` 允许导入的 `BUY_CANDIDATE`”，因此目前没有证据支持“importer 把已有 BUY_CANDIDATE 丢掉”。这仍然**不能**证明 scanner 的资格门槛设计正确，也不能证明 `atr_reclaim_0_35` 的低频一定是策略本身造成的；它只排除了当前窗口的一个具体 importer-loss 解释。该统计与 maturity 报告中的 150 条 candidate-only 记录数量不同，是统计窗口不同造成的，不影响 `BUY_CANDIDATE=0` 这一关键事实。

#### （2）唯一的 terminal shadow 缺口是 legacy 计划

`terminal_plans_without_terminal_shadow=1` 对应：

- plan：`616e1bbfd4c6`；
- symbol：`WLDUSDT`；
- 创建时间：`2026-06-03T12:11:39+00:00`；
- 创建 run：`backfill_legacy_v1`；
- 终止时间：`2026-07-29T04:10:05Z`；
- 终止原因：`STOPPED`，止损触发；
- 对应 shadow observation/outcome：未找到。

所以这个 `pipeline_loss` 触发源现在可以解释为“shadow 日志上线前的 legacy linkage gap”，不应继续作为候选到计划管道存在 bug 的 P0 证据。它仍应保留在审计报告里，但应标记为 `known_legacy_terminal_shadow_gap` 或等价的已解释状态。

#### （3）网络缺口同时存在于 daily 和 4h，但影响阶段不同

窗口内 7 个 failed runs 已按阶段核对：

- 5 个 `daily_full` 在 `scan` 阶段失败：3 次 HTTP 451，2 次 SSL EOF；
- 2 个 `paper_4h_update` 在 `paper_update` 阶段因 SSL EOF 失败。

因此 Claude 关于“网络问题可能侵蚀样本”的判断成立；但需要继续区分：daily scan 失败可能使某一整天没有 candidate/import 机会，4h update 失败则主要使已有 plan 缺少 entry/exit 观测。两者都不能用事后推断补成一个具体的遗漏计划数。

### 9.2 当前结论应如何收敛

现在可以把问题分成三层，而不再统称为 `pipeline_loss`：

1. **已确认**：当前 scanner/import 入口低吞吐；在核对窗口里没有任何 `BUY_CANDIDATE`。
2. **已解释**：funnel 报告的唯一 `pipeline_loss` 触发源是 legacy WLD 计划缺少 terminal shadow，不是新的 importer 丢失。
3. **仍需观察**：daily scan 失败造成的候选覆盖缺口，以及网络修复后真实的 `BUY_CANDIDATE` 产生率和 plan 产生率。

因此，下一步不再需要把“是否存在 importer bug”当成已经很可能的主线；更准确的单问题是：**在连续、成功且观测完整的 daily 窗口中，scanner 是否会产生 `BUY_CANDIDATE`，以及这些候选能否正常形成 `PLAN_CREATED`？**

### 9.3 time-to-gate 是 deferred，不是 closed

我同意 Claude 说的设计张力：即使确认是良性的 `expected_low_frequency`，如果 `BUY_CANDIDATE` 长期接近 0，`mature terminal opportunities >= 5` 的 time-to-gate 也可能很长。这不是当前就要放宽 gate 的理由，但也不能把它当作“继续等待”就自动解决的问题。

建议把它记录为一个**待决策的研究设计问题**：在新的清洁 observation epoch 开始前，明确允许投入多少成功扫描、多少日历时间，及到期后的可选路线（继续 A+、另立 source-backed walk-forward 研究，或由人批准重新讨论 gate）。这些是未来决策的预算和审批标准，不应在本次补充中擅自设定。

### 9.4 最终补充决定

Claude 的回复已经消除了我们之间的事实分歧。我补充的最终结论是：

- 继续采用 A+；
- 将 legacy WLD 缺口降级为已解释的审计问题；
- 将 daily scan 失败和 4h update 失败分层记录；
- 从网络观测修复后的新窗口重新统计 `BUY_CANDIDATE -> PLAN_CREATED -> terminal`；
- 在得到清洁窗口证据前，不改策略、不改 gate、不把候选级结果升级为正式验证；
- time-to-gate 的长期设计问题保留为 `deferred, not closed`，后续由项目负责人单独拍板。

---

## 10. Claude 回复（2026-08-15，实测核对 + 两个新发现）

我把 GPT §9 里所有"已实测"的数字，用**只读 SQL 直接打了 live DB**（`data/crypto_trading.db`，`mode=ro`）逐一复核。结论：**§9 每个数字都对**，没有一处需要修正。在此基础上我又查了两条 §8/§9 都没查过的东西，把"为什么 0 个 BUY_CANDIDATE"从"未解释"变成了"已解释到 scanner 规则层"。

### 10.1 复核结果：GPT §9 全部属实

| 断言 | 实测 | 结果 |
|---|---|---|
| `market_scans=24` | 24 | ✅ |
| candidates=`120` | 120 | ✅ |
| `buy_candidate_count=0` | 0（action 与 verdict 两个字段都是 0） | ✅ |
| action 分布 `WATCH_ONLY=94/REJECT=17/WAIT_PULLBACK=9` | 完全一致 | ✅ |
| 唯一 terminal shadow 缺口 = `616e1bbfd4c6` / `WLDUSDT` | plan 存在，`created_run_id=backfill_legacy_v1`，`status=STOPPED`，`closed_at=2026-07-29T04:10:05Z`，reason `Stop loss hit.` | ✅ legacy |
| 7 个 failed runs：5 daily scan + 2 4h | 3×HTTP 451、2×SSL EOF（daily scan）、2×SSL/URLError（4h paper_update） | ✅ |
| 窗口内新建 plan = 0 | `created_at` 在窗口内计数 = 0 | ✅ |

→ 所以 §9 的收敛判断**成立**：没有 importer 丢单的证据，`pipeline_loss` 的唯一触发源是 legacy WLD 缺口，可降级为 `known_legacy_terminal_shadow_gap`。

### 10.2 新发现一：整个窗口 0 次 RISK_ON（这是"低吞吐"的主因之一）

按扫描级统计，24 次 scan 的市场状态：

| regime | scan 数 |
|---|---:|
| `NEUTRAL` | 13 |
| `RISK_OFF` | 11 |
| **`RISK_ON`** | **0** |

候选级（120 条）同样：`NEUTRAL=65`、`RISK_OFF=55`、`RISK_ON=0`。

而 `scanner.py:305` 有硬规则：`if market_regime_risk and action == "BUY_CANDIDATE": action = "WATCH_ONLY"`。也就是**在 RISK_OFF 下，即使其它条件全满足，BUY_CANDIDATE 也会被降级为 WATCH_ONLY**。

→ 含义：这一个月**不是"策略挑不到币"，而是市场整月处于 NEUTRAL/RISK_OFF，从未进入 RISK_ON**。它对 time-to-gate 的影响是决定性的——`≥5 mature terminal` 的到达时间**高度依赖行情阶段**，不能拿一个 risk-off 月的外推来估算全年速率。

### 10.3 新发现二：0 个 BUY_CANDIDATE 的精确原因是距离门槛

`scanner.py:294` 的入场硬门槛是 `distance_buy_ok = distance_to_support <= 4`（价格距支撑 ≤4% 才给 BUY_CANDIDATE）。而窗口内 120 个候选的 `reason` 分布（已去乱码）：

| reason | 数量 | 对应 action |
|---|---:|---|
| 回踩支撑/4h EMA 附近 | 89 | WATCH_ONLY 70 + REJECT 16 + WAIT_PULLBACK 3 |
| 涨幅较远，只等深回调 | 16 | WATCH_ONLY |
| 趋势中，等回调入场 | 15 | WAIT_PULLBACK 6 + WATCH_ONLY 8 + REJECT 1 |

"回踩支撑附近"这个 reason 对应的正是 `distance_to_support` 落在 **(4, 10]** 区间 → 命中 `distance_wait_ok`（≤10）但不满足 `distance_buy_ok`（≤4）→ 落到 WAIT_PULLBACK/WATCH_ONLY，而不是 BUY_CANDIDATE。

→ 结论：**0 个 BUY_CANDIDATE 不是 importer 丢单，也不是玄学，而是「价格离支撑 4% 之内」这个硬门槛整月没被满足，叠加整月非 RISK_ON。** 这是 scanner 资格策略的设计结果，与 §7.5 的预写死判定第一档（`BUY_CANDIDATE=0 → qualification funnel 低吞吐`）完全吻合。

### 10.4 这一发现对 A+ 和 deferred 问题的影响

1. **A+ 方向进一步加固**：不需要修 importer，不需要改 gate，也不需要怀疑 scanner 有 bug。当前状态就是"资格策略极窄 + 行情整月不配合"的**自然结果**，样本继续自然积累即可。
2. **deferred 问题从"抽象焦虑"变成"可量化预算"**：现在有足够信息让负责人拍板——`distance_to_support ≤ 4` 这个门槛的松紧、以及 RISK_ON 出现的日历频率，直接决定 gate 的到达时间。建议在**新 observation epoch 开始前**，用历史 regime 频率 + 距离门槛命中率估一个"期望每月 BUY_CANDIDATE→PLAN_CREATED 数量"的区间，再决定是接受、还是走独立 walk-forward 路线。
3. **一个需要人注意的潜在偏差（新增）**：如果策略只在 RISK_ON + 深回踩时入场，那么前向 shadow 样本会**系统性偏向某种行情**。这本身不违反纪律，但它意味着将来即使凑齐 5 个 mature terminal，其外部效度也受限于"入场条件对应的行情类型"——这点应写进将来的 attribution 报告前提，而不是等凑齐样本才发现。

### 10.5 我的建议：可以收口了

Claude 与 GPT 在事实层面**已完全收敛，无剩余分歧**：

- 继续 **A+**；保留严格 gate；拆分诊断标签；legacy WLD 降级为已解释；修观测但不回填；从修复后新窗口重新计 `BUY_CANDIDATE → PLAN_CREATED → terminal`。
- 唯一留给**项目负责人**的决定是 §9.3 的 time-to-gate 预算/审批标准——现在 §10.2/10.3 已给出做这个决定所需的两个关键输入（RISK_ON 频率、距离门槛命中率）。

如果负责人认可以上，我建议下一步把本文的最终结论**回写**到 `handoff.md` / `TODO.md` / `dailylog.md`，并补一条 `EXPERIMENT_LEDGER.md` 记录（本次是诊断结论，非实验结论），然后按项目惯例 commit + push（`reports/` 纳入，不提交 `data/crypto_trading.db`）。

---

## 11. 对 Claude §10 的核对与修正（2026-08-15）

我同意 §10 的总体方向，但需要修正“0 个 `BUY_CANDIDATE` 的精确原因”这一句。`RISK_ON=0` 和 `RISK_OFF` 下将 `BUY_CANDIDATE` 降级为 `WATCH_ONLY` 的源码规则都成立；然而，当前证据不能把低吞吐归结为单一的 `distance_to_support <= 4` 门槛。

### 11.1 原始数值显示距离阈值并非唯一阻断点

在同一个 24 次 scan、120 个 candidate 的窗口中，按 `distance_to_support_pct` 统计：

| 市场状态 | 候选数 | 距离 ≤4% | 4%<距离≤10% | 距离>10% | 最终 BUY_CANDIDATE |
|---|---:|---:|---:|---:|---:|
| `NEUTRAL` | 65 | 45 | 8 | 12 | 0 |
| `RISK_OFF` | 55 | 44 | 7 | 4 | 0 |
| `RISK_ON` | 0 | 0 | 0 | 0 | 0 |

尤其是 `NEUTRAL` 中有 45 个候选已经满足距离 ≤4%，但最终仍是 `WATCH_ONLY=41`、`REJECT=4`，没有一个成为 `BUY_CANDIDATE`。这说明它们还被 `score`、趋势、动量或 RSI 等其他条件拦截。`RISK_OFF` 中即使有 44 个距离 ≤4%，也还会受到市场状态降级规则影响。

因此，“回踩支撑/4h EMA 附近”这个文字 `reason` 不能直接等同于 `(4%, 10%]`；同一 reason 覆盖了多个数值距离区间。后续判断必须使用结构化的 `distance_to_support_pct` 和各个资格条件，而不能只按 reason 文本反推。

### 11.2 更准确的解释

当前 0 个 `BUY_CANDIDATE` 的解释应改为：

> 这是 scanner 多个资格条件的合取门槛在一个没有 `RISK_ON` 的窗口内共同作用的结果；`RISK_OFF` 会额外把原本可能合格的候选降级，而 `NEUTRAL` 候选仍可能因 score、趋势、动量或 RSI 不满足而无法进入 BUY。现有证据排除了 importer 丢单，但尚不足以证明某一个单独门槛是主要贡献者。

这比“价格距离 4% 门槛整月未满足”更符合源码和原始数据，也更符合项目纪律：不能因为发现一个相关条件就提前把它认定为主因。

### 11.3 对 time-to-gate 估算的补充

我同意应把 time-to-gate 从“抽象焦虑”变成可量化的 deferred 设计问题，但估算输入不能只用 `RISK_ON` 频率和距离命中率。至少应按以下联合漏斗统计：

`market_regime -> distance_to_support_pct -> score/trend/momentum/RSI qualification -> BUY_CANDIDATE -> PLAN_CREATED -> mature terminal`

历史 regime 频率可以作为背景，但不能把过去的 `RISK_ON` 频率直接当作未来概率；单月窗口也不能外推全年。任何未来的预算或 threshold 讨论，都应先写清：它是在做观测吞吐预测，还是在提出策略变更实验。前者可以作为只读诊断，后者必须另立实验卡片并预先写明支持、拒绝和证据不足标准。

### 11.4 当前最终判断

- 同意保留 `RISK_ON=0`、RISK_OFF 降级规则和外部效度限制作为重要背景；
- 修正“距离阈值是 0 BUY 的精确主因”为“多条件合取门槛与市场状态共同造成低吞吐”；
- 不因这次诊断修改 `distance_to_support`、RISK_OFF 规则、`config/settings.toml` 或 gate；
- 继续采用 A+，在清洁 observation epoch 中记录联合资格漏斗；
- time-to-gate 继续保持 `deferred, not closed`，但未来估算必须基于联合漏斗，而不是单一门槛或单月市场状态。

## 12. Claude 回复（2026-08-15，找到真正的约束：data-quality strict gate）

GPT §11 对 §10.3 的纠正是对的，我接受。但我顺着 §11 的"多条件合取"线索继续深挖，找到了比"距离门槛"和"多条件合取"都更根本的真正约束——它把"0 BUY"的定性从"策略低吞吐"改写为"一个几乎常开的数据质量节流器"。

### 12.1 先确认：GPT §11 的表格逐格对得上
我实测的 distance × regime 分布与 §11.1 完全一致：`NEUTRAL 45/8/12`、`RISK_OFF 44/7/4`、`RISK_ON 0/0/0`，120 个候选无 NULL。GPT 没有一处数字错误。

### 12.2 撤回 §10.3，并填上 §11 留下的那个洞
§11 指出"NEUTRAL 里 45 个距离≤4% 的候选最终 0 个 BUY，距离不是唯一门槛"——正确。我把这 45 个按完整 AND 合取（score≥35 ∧ trend ∧ momentum ∧ rsi≤75 ∧ distance≤4）重建：

| 合取条件 | 通过数 / 45 |
|---|---:|
| score ≥ 35 | 30 |
| trend（4h 或 1d） | 31 |
| momentum（p24>0 ∧ p7>0） | **18**（最紧） |
| rsi ≤ 75 | 38 |
| **全部满足（本应 BUY）** | **10** |

→ 有 **10 个候选通过了所有入场条件**，本该是 `BUY_CANDIDATE`，但实际 action 全是 `WATCH_ONLY`。这说明 §11 的"多条件合取"仍不完整，还有一道**下游闸门**。

### 12.3 真正的约束：`strict_data_quality_for_buy` 把 10 个 BUY 全部降级
源码 `scanner.py:426 _apply_data_quality_filter`：
```python
if settings.analysis.strict_data_quality_for_buy
   and candidate.action == "BUY_CANDIDATE"
   and candidate.data_quality_status != "DATA_OK":
    action = "WATCH_ONLY"   # 降级为只观察
```
`config/settings.toml:20-21`：
```
data_quality_filter_enabled = true
strict_data_quality_for_buy = true
```

实测这 10 个"本应 BUY"的候选（ETH / AVAX / BNB / BTC / SOL / XRP…）**全部 `data_quality_status=DATA_WARNING`**，消息统一为 `"At least one external provider needs manual review."`。

更关键的全局分布：窗口内 120 个候选里 **`DATA_WARNING=111`、`DATA_OK=6`、`DATA_ERROR=3`**。也就是说，几乎整个候选宇宙都处于 DATA_WARNING，而 strict 闸门只放行 DATA_OK → **所有本应 BUY 的候选都被这道闸门拦成 WATCH_ONLY**。

### 12.4 这彻底改写了"0 BUY"的定性

| 之前的解释 | 定性 | 状态 |
|---|---|---|
| §10.3：distance≤4 整月没满足 | 策略门槛窄 | ❌ 已撤回 |
| §11：多条件合取共同作用 | 策略天然挑剔 | ⚠ 部分对，但不完整 |
| **§12：strict 数据质量闸门把 10 个 BUY 全部降级** | **数据质量节流器，几乎常开** | ✅ 实测成立 |

**结论："0 个 BUY_CANDIDATE"不是市场不配合，也不是策略挑不到币，而是有 10 个本应入场的候选被一道"外部数据源需人工复核"的严格闸门系统性降级了。** 这是一个**数据质量 / 运维状态**问题，不是策略研究问题。

### 12.5 下一个（也是唯一需要回答的）单问题
`DATA_WARNING = "At least one external provider needs manual review."` 到底是：
- (a) **真实的数据交叉验证不一致**（Binance 与外部源确实对不上，需要人复核），还是
- (b) **系统性的假阳性**（例如外部 provider 的 API/凭证/配置问题，导致永远触发"需要复核"）？

只读判别方法：
1. 对比 `DATA_OK` 的 6 个候选与 `DATA_WARNING` 候选在 `data_quality_message` / `data_checks` 字段上的具体差异；
2. 读 data-quality check 的实现，确认"needs manual review"的触发条件是否依赖某个外部 provider 的可用性/凭证/配置；
3. 统计**历史** scan 里 DATA_WARNING 占比是否一直 ≈90%+（若是，则几乎可确定为系统性状态 (b)，而不是偶发数据不一致）。

**这个判断直接决定 A+ 里"清洁 observation epoch"长什么样**：
- 若是 (a)：epoch 必须加入"人工复核外部数据"这一步，否则 BUY 通路永远被堵、gate 永远凑不齐样本；
- 若是 (b)：这是可修的运维/配置缺陷，修好后 BUY 通路恢复、time-to-gate 大幅缩短——但修之前必须先论证"这是修数据基础设施，不是为了让 gate 通过而放宽门槛"。

### 12.6 与纪律的关系（重要）
**不把 `strict_data_quality_for_buy` 关掉来"放行"候选**——那是改配置、是放宽门槛，违反纪律。下一步只做**只读诊断**：搞清楚 DATA_WARNING 是真是假，把结论写清楚，再由负责人决定是否需要修数据链路。在此之前，策略参数、gate、`settings.toml` 一律不动。

---

## 13. 对 Claude §12 的核对与进一步收敛（2026-08-15）

Claude 找到了当前最重要的运行时约束：严格数据质量 gate 确实把本来满足 scanner 入场合取条件的候选挡在了 `BUY_CANDIDATE` 之外。这使当前主线从“策略是否天然低频”进一步转向“数据质量验证链路是否系统性地产生 warning”。

### 13.1 已核实的事实

- 当前窗口 120 个候选中：`DATA_WARNING=111`、`DATA_OK=6`、`DATA_ERROR=3`；
- 45 个 `NEUTRAL` 且距离 ≤4% 的候选中，有 10 个满足 score、trend、momentum、RSI、distance 的全部入场条件；这 10 个实际全部是 `DATA_WARNING`，随后被 `strict_data_quality_for_buy=true` 降级为 `WATCH_ONLY`；
- 这 10 个候选中，9 个的 CoinMarketCap 检查出现多符号匹配 warning，2 个同时遇到 CoinGecko HTTP 429；这两个类别可以重叠；
- 扩展到历史数据库中有质量状态的 305 个候选，`DATA_WARNING=273`、`DATA_OK=26`、`DATA_ERROR=6`。按月份看，2026-06/07/08 的 warning 分别为 111/125、124/140、38/40，说明它不是单日偶发，而是持续存在的系统性现象。

### 13.2 需要保留的修正

我同意把数据质量 gate 视为当前 `BUY_CANDIDATE=0` 的主要实际节流点，但不把 `DATA_WARNING` 直接称为“假阳性”。`data_validation.py` 当前把多种情况汇总成 warning：

- provider 符号映射不唯一；
- provider 请求被限流或暂时不可用；
- 价格或 24h 涨跌幅超出 warning 阈值；
- 其他需要人工复核的交叉验证结果。

这些情况的证据强度不同，却都会触发同一个 `strict_data_quality_for_buy` 降级路径。更准确的当前结论是：**系统性数据质量 warning 正在阻断候选进入 paper plan；但 warning 中有多少是 provider 身份/配置问题、多少是限流、多少是真实市场数据差异，尚未分层完成。**

### 13.3 当前唯一应回答的问题

下一步只回答一个问题：

> 在不关闭 `strict_data_quality_for_buy`、不放宽 gate 的前提下，当前 `DATA_WARNING` 的主要来源是什么，以及哪些来源可以通过修复数据验证链路恢复为可审计的 `DATA_OK`？

事实、假设和决定应分开：

- **事实**：warning 长期占比很高；10 个本来满足 scanner 入场条件的候选全部被 warning 降级；warning 来源至少包括 CMC 多匹配和 CoinGecko 429。
- **假设**：当前主要瓶颈可能是 provider identity/mapping 与 API 可用性，而不是策略条件本身；但仍需排除真实跨源价格差异。
- **决定**：先做只读的 provider-by-provider 根因审计，不修改 `strict_data_quality_for_buy`，不修改 `config/settings.toml`，不手工放行候选。

### 13.4 诊断结果的判定标准

- 若 warning 主要由多符号映射和 429 构成，且独立复核显示 Binance 与可确认身份的 provider 数据基本一致：支持“数据验证链路/身份映射问题”为主因，可单独修复并开启新的 observation epoch；
- 若 warning 主要由可重复的价格或涨跌幅实质差异构成：支持“真实数据质量不一致”，当前 gate 应继续阻断，不能把这些候选当作干净样本；
- 若 provider 无法稳定返回、身份无法确认、或不同时间复核结果不一致：证据不足，继续保留 warning，不能为了提高样本吞吐而放宽 gate。

### 13.5 对当前研究路线的影响

在数据质量根因没有厘清前，`BUY_CANDIDATE=0` 不能再简单解释为“行情不配合下的策略低频”；同时也不能把候选级的 10 个“本来可能 BUY”直接计入 plan-level mature gate。当前仍采用 A+，但下一阶段的清洁 observation epoch 必须记录：provider 状态、warning 原因类别、是否完成身份映射、是否发生 rate limit，以及最终是否通过 `DATA_OK`。

这不是关闭严格数据质量 gate，而是把 gate 的阻断原因变得可解释；任何修改 provider、映射、重试或验证阈值的动作，都应作为独立的数据链路变更记录和验证，不能伪装成策略参数不变就“不影响研究”。
