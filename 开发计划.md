---
created: 2026-05-19
updated: 2026-06-13
tags:
  - crypto
  - trading-system
  - codex
  - project-plan
status: in-progress
---

## 2026-07-27 Stage A-E 执行结论

根据 GPT 评审后的计划，已完成 Stage A closure audit，并复核 Stage B/C/D 的既有证据覆盖范围。`replacement_closure_audit` 显示 Stage 4 comparison sample 高度集中：42 个 eligible comparison events 只对应 3 个 unique stale trades，top1 stale trade 占 83.333%，按 stale trade 去重后 R42 median 约 -0.004，cluster bootstrap R42 p05 为 -0.565。因此 capacity replacement 分支正式冻结为 `paused_no_stable_executable_edge`，不进入 Stage 5 / Stage E shadow replacement，不提高 `max_active_positions`，不修改 `config/settings.toml`。

`atr_reclaim_0_35` 的正式 A/B 仍显示两段净收益、PF、Sharpe、MDD 改善，但交易级归因确认改善主要来自 variant-only 新增赢家，而不是 common trades 广泛变好；阈值敏感性中 `0.35` 是当前最强点，但还不是可直接 keep 的稳定平台。当前状态为 `retest_path_dependent`。下一阶段若继续研究，只能从 capacity-neutral 的单变量 entry-quality retest 开始，先提出实验卡片和通过/否决标准，不叠加 replacement 或其它过滤器。

## 2026-07-26 研究阶段校准

当前暂停新增参数实验，先进入“理解和盘点”阶段。新增 `SYSTEM_OVERVIEW.md`、`EXPERIMENT_LEDGER.md`、`RESEARCH_ROADMAP.md` 作为三份主索引：分别解释系统结构与策略逻辑、既有实验账本、后续研究路线。

阶段判断：基础设施已基本可用，策略质量研究正在进行，但系统复杂度已经足以产生黑箱风险。下一步优先让项目所有者能独立解释“策略为什么可能赚钱、当前实验在验证什么、结果说明什么、下一步为什么这样做”，而不是继续叠加新过滤器。

默认策略纪律：`retest` 不等于 keep，`candidate_keep_review` 不等于自动部署；未经用户批准，不修改 `config/settings.toml`。任何新实验必须先提交实验卡片，说明唯一问题、固定条件、判定标准、风险和是否允许改生产配置。

# CryptoTradingSystem 开发计划

## 项目目标

用 Codex 辅助开发一个本地加密货币交易研究系统。当前目标不是自动实盘下单，而是先建立一套可复核、可回测、可模拟盘跟踪、可持续优化的交易研究流程。

第一阶段核心目标：

> 每天自动扫盘 -> 生成候选币和交易计划 -> 数据交叉验证 -> 模拟盘跟踪 -> 回测验证 -> 复盘并优化策略。

## 设计原则

- 先模拟盘，后实盘。
- 先风控，后收益。
- 先可解释信号，后自动化执行。
- 不把 LLM 当成直接下单黑箱。
- 每个买入候选必须包含入场区间、止损、止盈、风险收益比和失效条件。
- 每次策略规则变更必须通过 A/B 回测验证，不能凭感觉修改默认策略。
- `closed_trades < 20` 的回测结果必须标记为样本不足，不能作为 keep 依据。

## 当前总体状态

系统已经完成 MVP，并进入“策略质量优化 + 三周模拟盘结构化观察”阶段。

2026-07-14 经 GPT 复审当前进场方案后，结论为“修改后可以执行”：不推倒重来，也不在 2026-07-16 checkpoint 前直接修改 `settings.toml` 或 live/paper 进场规则。下一优先级从“是否直接启用某个过滤器”调整为“先补强离线实验归因口径”：所有 entry 相关 shadow replay 需要按 scanner、entry trigger、regime、exit 分层，加入 R multiple、MFE/MAE、missed winner / filtered loser 的 R 加权机会成本，并补充 `distance_to_support / ATR`、`reclaim_margin / ATR` 等 ATR 标准化字段。后续实验优先级为 `reclaim_quality_matrix`、`momentum_pullback_definition_ab`、`relative_strength_soft_gate`；MACD 暂不升级为硬门槛，只作为低优先级离线变体研究。

2026-07-25 已补跑 2026-07-03 -> 2026-07-16 paper checkpoint review。窗口达到 `formal_audit_ready`，但数据链路为 `partial_pass`，右截尾比例仍有 40.0%；formal audit 显示 `defense_net_R=-24.35`，`missed_winner=11` 高于 `avoided_loser=7`，同时 entered trades 中 5 笔归因为 `entry_issue`、2 笔归因为 `selection_issue`。结论：当前证据足以说明防守规则存在机会成本和入场质量问题，但不足以直接启动正式 A/B 或修改默认配置。下一步保持 `settings.toml` 冻结，优先补强 shadow replay 统计口径、ATR 标准化字段和固定 opportunity set，然后按 `reclaim_quality_matrix`、`momentum_pullback_definition_ab`、`relative_strength_soft_gate` 顺序推进离线实验。

同日已补齐上述四个前置条件并启动三项实验：扩展窗口改为 `2026-07-03 -> 2026-07-25` 后，`right_censored_ratio` 降至 21.6%；`paper shadow-replay` 已输出 R 汇总和分层统计；`OpportunityRow` 已补 ATR 标准化字段；新增 `paper shadow-experiment` 固定样本 JSON 和 `opportunity_set_hash`。首轮三个实验均使用 `9468fbe1bab35767`，结论均为 `retest`，不修改 `settings.toml`。当前更值得后续复测的方向是 `relative_strength_soft_gate` 中的 `btc_eth_soft_minus_0_5` / `btc_eth_hard_0`，以及 `momentum_pullback_definition_ab` 中的 `trend_support_atr_pullback`；`reclaim_quality_matrix` 里 `confirm_1bar` 因错过 4 个 winner 暂不优先。

同日深夜已完成跨窗口复核：`2026-06-19 -> 2026-07-02` 可作为旧窗口对照，`2026-07-03 -> 2026-07-25` 是主证据，`2026-07-17 -> 2026-07-25` 因 `right_censored_ratio=44.0%` 只能作为 interim direction check。三项 fixed opportunity shadow experiment 结论仍为 `retest`，不修改 `settings.toml`。下一步若进入正式 A/B，优先做单变量 `relative_strength_soft_gate` / `btc_eth_soft_minus_0_5`，其次才考虑 `reclaim_quality_matrix` / `atr_reclaim_0_25` 与 `momentum_pullback_definition_ab` / `trend_support_atr_pullback`。

2026-07-26 已将 `relative_strength_soft_gate` 推进为正式 dynamic-universe A/B：新增 loader 默认关闭的 `relative_strength_soft_gate_enabled=false` 与 `relative_strength_min_pct=-0.5`，实验 `relative_strength_soft_gate_btc_eth_minus_0_5` 只打开这一项，`settings.toml` 保持冻结。两段非重叠 walk-forward 均样本充足且无重叠，净收益、PF、Sharpe、止损率均改善；但早期窗口最大回撤从 16.59% 升到 18.96%，因此汇总结论保持 `retest`，不部署。下一步优先复核早期窗口 MDD 恶化来源，再决定是否继续研究相对强度门槛阈值或转向 `atr_reclaim_0_25`。

同日已完成早期窗口 MDD 复核：`relative_strength_soft_gate_btc_eth_minus_0_5` 的 MDD 恶化主要来自路径依赖，而不是单笔极端亏损。variant-only trades 总体净贡献 +565.32，11 月新增 winner cluster 贡献 +2,089.38 并抬高权益峰值，但随后 12 月、1 月、5 月新增止损簇造成更深 peak-to-trough。该规则保持 `retest`；下一步若继续研究，应做同一维度阈值敏感性测试（例如 `-1.0` 与 `0.0`），而不是直接部署或叠加其它 entry 过滤器。

同日完成 `relative_strength_soft_gate` 阈值敏感性：`-1.0` 太松，近端窗口 PF/Sharpe/MDD/净收益退化；`0.0` 近端窗口最强，但早期窗口 MDD 恶化到 20.58%，且早期净收益改善弱于 `-0.5`；`-0.5` 仍是最平衡阈值，但早期 MDD 未解决。因此相对强度门槛家族整体保持 `retest`，不部署。下一轮正式单变量 A/B 转向 `reclaim_quality_matrix / atr_reclaim_0_25`，继续保持不叠加其它 entry 过滤器。

同日已完成 `reclaim_quality_matrix / atr_reclaim_0_25` 正式 dynamic-universe A/B：新增默认关闭的 `entry_reclaim_min_atr_enabled=false` 与 `entry_reclaim_min_atr=0.0`，实验 variant 要求 4h 收盘超过 `entry_high + 0.25 * ATR` 才允许入场，`settings.toml` 保持冻结。两段非重叠窗口均样本充足且无重叠，净收益、PF、Sharpe、胜率均改善；近端窗口 MDD 从 20.75% 降至 15.01%，但早期窗口 MDD 从 16.59% 升至 19.21%，近端 stop rate 也小幅升高。因此结论为 `retest`，不部署；下一步应做同维度阈值敏感性（`0.10`、`0.15`、`0.35`），而不是叠加相对强度过滤器。

同日已完成 ATR reclaim 阈值敏感性：`0.10` 和 `0.15` 虽改善早期窗口，但近端窗口净收益和 PF 退化；`0.25` 两段收益/PF 改善但早期 MDD 恶化；`0.35` 是当前最强阈值，两段净收益、PF、Sharpe、MDD 和 stop rate 均改善，自动汇总结论进入 `candidate_keep_review`。随后完成 `atr_reclaim_0_35` 交易级归因复核、10 笔关键路径复盘和 `capacity_and_opportunity_order_review`：合并后 common trades 小幅变差，改善主要来自 variant-only 新增赢家；这些赢家 reclaim margin 均超过 0.35 ATR，但 missed baseline winners 也有高质量 TP2 交易，且满仓/长持仓确实影响路径。容量复核同时显示两组都有长期负 R 占槽问题，但 blocker 质量混合，不能据此修改 `max_active_positions` 或 score 排序。因此当前仍不修改 `settings.toml`，状态保持 `retest_path_dependent`。GPT 二次评审后，下一步路线收紧为 `signal_fill_timing_audit -> blocked_entry_event_export -> replay_consistency_audit -> stale_slot_continuation_review -> blocked_candidate_vs_stale_slot_review`，先审计时点和事件标签，再考虑换仓价值，不继续调 ATR 阈值。

2026-07-27 已完成 `signal_fill_timing_audit` Stage 0：新增只读研究命令 `python main.py research signal-fill-timing-audit --run-id ...`，对 canonical candidate run `110c51eef593` 生成报告。审计确认 replay 顺序可检查：active exits 先于 WATCHING entries，WATCHING 按 `(-score, created_index, symbol)` 排序，capacity check 在 entry-zone touch、reclaim、sizing 之后；但 `step_trade` 可在同一次调用内让 WATCHING 入场并继续评估同根 stop/TP，结论为 `timing_audit_warn_same_bar_ambiguity`。下一步可以设计 `blocked_entry_event_export`，但必须记录 `fill_time_assumption`、`active_snapshot_after_exits`、`same_bar_entry_exit_possible` 和 `same_bar_entry_tp1_possible`。

2026-07-27 已完成 `blocked_entry_event_export` Stage 1：新增只读研究命令 `python main.py research blocked-entry-event-export --run-id ...`，对 source run `110c51eef593` 复跑得到 `replay_run_id=ed682b4a5531`，source/replay entered trades 均为 58，并导出 512 个 `block_reason=max_active_positions` 事件到 `reports/2026-07-27/blocked_entry_event_export_2026-07-27_v1.md` 与 JSON sidecar。该阶段只证明 blocked event 样本可被审计导出，不证明 replacement 有价值；下一步必须做 `replay_consistency_audit`，确认 entry time、active count path、候选排序和事件重复运行一致，再进入 stale slot 或 replacement outcome 分析。

2026-07-27 已完成 `replay_consistency_audit` Stage 2：新增只读研究命令 `python main.py research replay-consistency-audit --run-id ... --blocked-events-json ...`，对 source run `110c51eef593` 复跑得到 `replay_run_id=1e3cbb13c14a`。审计确认 trades `389 -> 389`、entered trades `58 -> 58`、closed trades `388 -> 388`、active/open-plan path 2190 点 mismatch 均为 0、final equity delta 为 0、blocked event repeat `512 -> 512` 且 signature mismatch 为 0。结论为 `replay_consistency_pass_with_ordering_limit`：可以进入下一阶段诊断，但候选排序不是 source 原生持久化字段，只能通过源码排序 marker 与重复 blocked-event 签名间接验证。下一步执行 `stale_slot_continuation_review`，先独立评估 pre-TP1 stale slots 达到 42 bars 后继续持有的边际价值，不直接计算 replacement outcome。

2026-07-27 已完成 `stale_slot_continuation_review` Stage 3：新增只读研究命令 `python main.py research stale-slot-continuation-review --run-id ... --stale-bars 42`，对 source run `110c51eef593` 复跑得到 `replay_run_id=8e24e6bda89b`。样本定义为已入场、达到 `42 bars = 168h` 时仍 pre-TP1 且未关闭的旧仓位；58 笔 entered trades 中有 26 个合格 stale slots，13 笔在 stale 前已 TP1，19 笔在 stale 前已关闭，右截尾 1 笔。继续持有增量 R 为负：`forward_R_24_mean=-0.282`、`forward_R_42_mean=-0.132`、`forward_R_60_mean=-0.191`、`eventual_continuation_R_mean=-0.129`；first-hit outcome 为 stop 15、tp1 10、not_hit_by_end 1。结论为 `stale_slot_continuation_weak_retest`：旧仓继续占槽确实有弱点，支持进入 `blocked_candidate_vs_stale_slot_review`，但仍不部署、不提高 `max_active_positions`、不修改 replacement 状态机。

2026-07-27 已完成 `blocked_candidate_vs_stale_slot_review` Stage 4：新增只读研究命令 `python main.py research blocked-candidate-vs-stale-slot-review --run-id ... --stale-bars 42`，对 source run `110c51eef593` 复跑得到 `replay_run_id=e40da6f04438`。主样本只取 `candidate_rank=1` 的 blocked candidate，并用事前规则选择同一 event 中最老的 pre-TP1 且 `holding_bars >= 42` 的 stale slot；512 个 blocked events 中 rank1 events 为 46，合格比较事件为 42，右截尾 0。结果显示短期 `net_delta_R_24_mean=0.436` 且 positive ratio 59.5%，但主指标 `net_delta_R_42_mean=0.309`、median `-0.223`、positive ratio 42.9%，`net_delta_R_60` median 继续为负，20% trimmed mean 约 `0.001`；lowest-unrealized sensitivity 的 R42 median 仍为 `-0.276`，只有 oracle upper bound 明显为正。结论为 `replacement_edge_not_supported`：不进入 Stage 5 shadow replacement，不部署 replacement，不提高 `max_active_positions`；后续若重启容量研究，必须先提出更强且事前声明的 slot selection 规则或更广 walk-forward 证据。

当前已经具备：市场扫盘、数据质量过滤、数据交叉验证、候选币交易计划生成、人工复核报告、模拟盘、回测模块、universe snapshot 回测、Dynamic Universe Backtest MVP、A/B 实验框架、报告按日期和版本输出、GitHub 同步。

2026-06-13 已完成 SQLite 主数据层切换：schema v2 为 `paper_plans` 补齐完整模拟盘运行态，paper update、paper report 和 observation dashboard 均从 `paper_plans/paper_events` 读取；旧 `paper_trades/paper_trade_events` 仅保留为同事务兼容镜像。生产库已迁移，25 个计划和 57 个事件的新旧层数量一致，结构化计划没有缺失运行态字段。下一阶段先连续观察 5 天，确认稳定后再启用 4h paper update 计划任务。

2026-06-13 已补齐 4h cycle 与锁竞争的运行级验收：临时库集成测试证明 `paper_4h_update` 只推进已有计划，写入 run/event/snapshot 和独立 report/dashboard，不新增 scan 或 plan；锁竞争测试证明业务写入遵守 busy timeout，超时后 run 会记录为 `failed` 并保留 `database is locked` 原因。

同日补齐 per-plan 原子回滚验收：故障注入证明某个 plan 在更新状态后写 event 失败时，该 plan 的状态、事件和 snapshot 会整体回滚；循环仍处理下一 plan，最后将整轮明确标记失败，符合“局部隔离、整体不伪装成功”的要求。

20:05 首个新版样本运行前修复了 Windows 日志编码：daily/4h 批处理现在统一使用 PowerShell 执行器按 UTF-8 写日志并传递真实退出码，历史混合编码日志已原样归档；计划任务仍调用同一 `.bat` 路径，无需管理员重装。

复盘入口也已补齐：`paper db-summary` 现在直接输出三周核心总量与 daily/4h 北京时间覆盖日期，不需要人工从通用事件表或 Markdown 报告二次拼接统计。

失败诊断现已覆盖完整运行步骤：daily 的 scan/add/update/report/dashboard、4h cycle 的 update/report/dashboard 和手工 paper update 在异常时都会把 `run_id` 与 `step` 写入错误，确保日志和 runs 表可直接定位失败阶段。

4h 更新脚本和管理员安装脚本已经准备完毕，但安装脚本受 `python main.py db stability --days 5` 硬门槛保护。2026-06-13、2026-06-14 两个新版 daily_full 样本均成功，当前稳定性进度 `2/5`；每天有 1 个 scan、4 个 snapshot、三类报告，无锁、重复或外键错误。ONDOUSDT 在两天均记录 `RECLAIM_PENDING_SET` 并保持 WATCHING，证明 pending 状态可跨日持久化。继续等待 2026-06-15 至 2026-06-17 的 3 个连续样本，门槛通过后再注册 Windows 任务。

同日完成 sensitive 组合叠加 `max_holding_42x4h_no_tp1` 的两段非重叠 walk-forward。两段 PF、Sharpe 和净收益均改善，近端 MDD 明显下降，但较早窗口 MDD 从 18.03% 升至 20.66%，因此保持 `retest`。95 笔 `TIME_EXIT` 的后续一周中，33 笔先触原 stop、15 笔先触原 TP1、60 笔期末低于退出价；固定 42 根规则具备止血价值，也会损失少数延迟赢家。下一优先级是条件式 42 根退出，不修改当前模拟盘默认配置。

当前主要问题：

- 策略交易质量仍需优化。
- 回测显示止损率偏高，Profit factor 不稳定。
- 需要逐项 A/B 测试选币规则，避免一次性叠加多个过滤导致无法归因。
- 当前固定 symbols 回测对部分选币过滤参数没有区分度；已补上 Dynamic Universe Backtest MVP，可以每日用历史 K 线重建 universe，但仍未解决历史已退市币不在当前 `exchangeInfo` master list 中的幸存者偏差。

## 已完成模块

| 模块 | 状态 | 说明 |
|---|---|---|
| 项目初始化 | 已完成 | 已建立 Python 工程、配置文件、SQLite、README、GitHub 仓库 |
| Codex Skills | 已完成 | 已建立 CryptoTrading 相关 skills |
| Binance 行情模块 | 已完成 | 支持现货 USDT 交易对、ticker、1h/4h/1d K 线 |
| 数据质量过滤 | 已完成 | 过滤稳定币、杠杆币、低流动性币、异常交易对 |
| 技术指标模块 | 已完成 | EMA、RSI、MACD、ATR、成交量变化 |
| 市场扫描器 | 已完成 | 可生成 Top N 候选币和交易计划 |
| 交易计划生成 | 已完成 | 入场区间、止损、TP1、TP2、R/R、失效条件 |
| 数据交叉验证 | 已完成 | Binance + CoinGecko + CoinMarketCap 可选 API 对照 |
| 验证池补位 | 已完成 | 先验证 `min(top_n * 2, 10)`，再按 action + score 补足最终名单 |
| 人工复核证据 | 已完成 | 报告含交易所链接、搜索链接、指标表、K 线证据、推导公开 |
| 模拟盘模块 | 已完成 | 支持 WATCHING、ENTERED、TP1_HIT、STOPPED、CLOSED、INVALIDATED |
| 模拟盘去重归档 | 已完成 | 同币种新计划可替换旧的未入场 WATCHING 计划 |
| 模拟盘导入口径 | 已完成 | 默认只导入 `BUY_CANDIDATE` |
| Daily / Doctor 命令 | 已完成 | 支持日常运行和环境检查 |
| 报告目录结构 | 已完成 | `reports/YYYY-MM-DD/` 和 Obsidian `Reports/YYYY-MM-DD/` |
| 报告版本号 | 已完成 | 同一天多次运行使用 `v1`、`v2`、`v3` |
| 回测模块基础 | 已完成 | 历史 K 线缓存、ticker 重建、状态机复用 |
| 回测成本模型 | 已完成 | 手续费、滑点、止损优先 intrabar 假设 |
| 回测指标 | 已完成 | 胜率、止损率、Profit factor、最大回撤、Sharpe、Sortino 等 |
| 回测报告中英对照 | 已完成 | 关键英文指标后附中文解释 |
| 回测 sample_sufficient | 已完成 | `closed_trades < 20` 自动标记样本不足 |
| Dynamic warmup | 已完成 | 回测预热覆盖 `min_history_days + 60` 根日线 |
| 大盘环境过滤 | 已完成 | BTC/ETH 弱势时山寨币买入候选降级 |
| 选币扣分参数化 | 已完成 | 追高扣分、高波动扣分已配置化 |
| A/B 实验框架 | 已完成 | `python main.py abtest --experiment ...` |
| 实验定义文件 | 已完成 | `config/experiments.toml` |
| Universe snapshot 回测 | 已完成 | `python main.py backtest-universe ...`；使用当前 Binance 快照选币，并在报告中记录快照元数据和幸存者偏差警告 |
| Dynamic Universe Backtest MVP | 已完成 | `python main.py backtest-dynamic-universe ...`；每日用已收盘历史 K 线重建 universe，报告记录 dynamic universe summary |
| K 线无数据负缓存 | 已完成 | Binance 返回空 K 线批次时记录 `kline_unavailable_ranges`，后续同区间不再重复请求 |
| A/B 多时段汇总报告 | 已完成 | `python main.py abtest-summary ...`；从既有 A/B Markdown 报告解析 Raw Metrics 并生成跨时段 keep/retest/reject 辅助结论 |
| A/B walk-forward 编排 | 已完成 | `python main.py abtest-walk-forward ...`；顺序运行多个 A/B 时段并生成仅包含本次运行报告的多时段汇总 |
| A/B 时段重叠分析 | 已完成 | 多时段汇总输出 `total_period_days`、`unique_coverage_days`、`overlap_periods`，重叠窗口保持 `retest` |
| A/B universe 偏差提示 | 已完成 | 多时段汇总输出 `Universe Bias Checks`，提示当前 `exchangeInfo` master、历史退市币缺失和 `source_limit` 截断风险 |
| 固定 dynamic symbol master | 已完成 | `--write-symbol-master` / `--symbol-master-file` 支持保存和复用同一份 dynamic universe master，提升 A/B 可复现性 |
| Dynamic symbol master 导出 | 已完成 | `python main.py dynamic-symbol-master --output ...` 可单独导出固定 master JSON；当前已生成 `reports/2026-06-09/dynamic_master_source150.json` 和不截断的 `reports/2026-06-09/dynamic_master_full.json` |
| GitHub 同步 | 已完成 | 已 push 到 `Goodyenough/wbj-CryptoTrading` |

## 当前可用命令

```powershell
python main.py doctor
python main.py scan --top 5
python main.py scan --top 5 --no-obsidian
python main.py verify --symbol BTCUSDT
python main.py daily
python main.py paper add-from-scan
python main.py paper update
python main.py paper report
python main.py backtest --symbols BTCUSDT,ETHUSDT,SOLUSDT --start 2025-01-01 --end 2025-06-01 --no-obsidian
python main.py backtest-universe --start 2025-01-01 --end 2025-02-01 --max-symbols 3 --no-obsidian --allow-data-gaps
python main.py backtest-dynamic-universe --start 2025-01-01 --end 2025-02-01 --source-limit 20 --max-symbols 5 --no-obsidian --allow-data-gaps
python main.py abtest --experiment history_250 --symbols BTCUSDT,ETHUSDT,SOLUSDT --start 2025-01-01 --end 2025-06-01 --no-obsidian
python main.py abtest --experiment history_250 --dynamic-universe --start 2025-01-01 --end 2025-02-01 --source-limit 20 --max-symbols 5 --no-obsidian --allow-data-gaps
python main.py abtest-summary --experiment liquidity_50m --mode dynamic_universe --reports-date 2026-06-09 --no-obsidian
python main.py abtest-walk-forward --experiment liquidity_50m --dynamic-universe --periods 2025-01-01:2025-09-01,2025-06-01:2026-06-01 --source-limit 100 --max-symbols 30 --allow-data-gaps --no-obsidian
python main.py abtest --experiment liquidity_50m --dynamic-universe --start 2025-06-01 --end 2026-06-01 --source-limit 150 --max-symbols 40 --write-symbol-master reports/2026-06-09/dynamic_master_source150.json --allow-data-gaps --no-obsidian
python main.py dynamic-symbol-master --source-limit 150 --output reports/2026-06-09/dynamic_master_source150.json
python main.py abtest-walk-forward --experiment liquidity_50m --dynamic-universe --periods 2025-01-01:2025-06-01,2025-06-01:2026-06-01 --symbol-master-file reports/2026-06-09/dynamic_master_source150.json --max-symbols 40 --allow-data-gaps --no-obsidian
python main.py dynamic-symbol-master --output reports/2026-06-09/dynamic_master_full.json
python main.py abtest --experiment liquidity_50m --dynamic-universe --start 2025-06-01 --end 2026-06-01 --symbol-master-file reports/2026-06-09/dynamic_master_full.json --max-symbols 40 --allow-data-gaps --no-obsidian
python main.py abtest-summary --experiment risk_off_no_core_top_n_3 --mode dynamic_universe --reports-date 2026-06-09 --no-obsidian
python main.py abtest-summary --experiment risk_off_no_core_top_n_3 --mode dynamic_universe --reports-date 2026-06-09 --drop-overlap-periods --no-obsidian
```

## 进行中：Priority 1 选币策略优化

当前阶段目标：先优化选币质量，暂不改买入、止损、TP1、TP2。

### 已完成的选币优化基础设施

- [x] 数据质量过滤：非 `DATA_OK` 买入候选降级。
- [x] 历史长度过滤：默认至少 180 根 1d K 线。
- [x] 大盘环境过滤：BTC/ETH 弱势时山寨币买入候选降级。
- [x] 数据质量验证池补位：验证更大候选池后再输出最终 top_n。
- [x] Dynamic warmup：回测预热不再制造早期无信号盲区。
- [x] 追高扣分参数化。
- [x] 高波动扣分参数化。
- [x] 模拟盘只导入 `BUY_CANDIDATE`。
- [x] A/B 实验框架。

### A/B 实验状态

| 实验 | 状态 | 类型 | 说明 |
|---|---|---|---|
| `history_250` | 已完成 3 个时段 A/B，暂不 keep | 配置实验 | `180 -> 250`；三段结果与 baseline 基本无差异，继续保留为 retest 证据 |
| `history_365` | 已完成首段，待扩大样本 | 配置实验 | `180 -> 365`；在 BTC/ETH/SOL 的 2025-01 到 2025-06 窗口无差异，样本不足 |
| `pump_chase_strict` | 已完成首段，待扩大 symbols | 配置实验 | 在 BTC/ETH/SOL 首段无差异，说明主流币样本对追高扣分不敏感 |
| `liquidity_50m` | 已完成首段，待扩大 symbols | 配置实验 | 在 BTC/ETH/SOL 首段无差异，主流币天然满足流动性门槛 |
| `risk_off_no_core_buy` | 已实现，待 full master A/B | regime 实验 | 在 `RISK_OFF` 下取消 BTC/ETH 核心币豁免，测试弱市是否应完全暂停新开仓 |
| `top_n_3` | 已实现，待 full master A/B | capacity 实验 | 将 `market.top_n` 从 5 降到 3，测试减少同日相关拥挤开仓是否改善 `RISK_ON` |
| `risk_off_no_core_top_n_3` | 已实现，待 full master A/B | 组合实验 | 合并弱市暂停核心币新开仓与降低候选容量，验证互补规则是否接近转正 |
| `entry_reclaim_close` | 已完成 full master 与非重叠 walk-forward，结论 `retest` | entry_timing 实验 | 入场区触碰后要求 4h 收盘重新站上 `entry_high`，近端转正但早期样本不足 |
| `risk_off_no_core_entry_reclaim` | full master 已跑完，待非重叠 walk-forward | combined_regime_entry 实验 | 合并弱市停开 BTC/ETH 核心币与 4h 入场重新确认，full master 接近打平但 `RISK_ON` 仍未转正 |
| `tp1_breakeven_stop` | full master 已跑完，结论 `reject_candidate` | exit_timing 实验 | TP1 命中后将止损移动到入场价，收益更差且回撤不改善，不进入 walk-forward |
| `history_three_stage` | 未实现 | 结构性实验 | 极短历史硬拒绝、指标稳定历史、短历史扣分 |
| `daily_trend_required` | 未实现 | 结构性实验 | BUY_CANDIDATE 必须满足日线强趋势 |
| `volatility_without_trend_penalty` | 未实现 | 结构性实验 | 高波动且趋势未确认时加重扣分 |

### 当前最新 A/B 结果（2026-06-06）

| 实验 | 时间段 | baseline closed | variant closed | PF baseline | PF variant | 净收益 baseline | 净收益 variant | 结论 |
|---|---|---:|---:|---:|---:|---:|---:|---|
| `history_250` | 2025-01-01 -> 2025-06-01 | 12 | 12 | 0.49 | 0.49 | -5.63% | -5.63% | 样本不足，retest |
| `history_365` | 2025-01-01 -> 2025-06-01 | 12 | 12 | 0.49 | 0.49 | -5.63% | -5.63% | 样本不足，retest |
| `pump_chase_strict` | 2025-01-01 -> 2025-06-01 | 12 | 12 | 0.49 | 0.49 | -5.63% | -5.63% | 样本不足，retest |
| `liquidity_50m` | 2025-01-01 -> 2025-06-01 | 12 | 12 | 0.49 | 0.49 | -5.63% | -5.63% | 样本不足，retest |
| `history_250` | 2024-06-01 -> 2024-12-31 | 23 | 23 | 0.85 | 0.85 | -3.27% | -3.27% | 无差异，暂不 keep |
| `history_250` | 2025-06-01 -> 2026-06-01 | 23 | 23 | 0.71 | 0.71 | -4.70% | -4.70% | 无差异，暂不 keep |
| dynamic `history_365` | 2025-01-01 -> 2025-04-01 | 5 | 5 | 0.00 | 0.00 | -5.13% | -5.13% | 样本不足，retest |
| dynamic `pump_chase_strict` | 2025-01-01 -> 2025-04-01 | 5 | 5 | 0.00 | 0.00 | -5.13% | -5.13% | 样本不足，retest |
| dynamic `liquidity_50m` | 2025-01-01 -> 2025-04-01 | 5 | 3 | 0.00 | 0.00 | -5.13% | -3.07% | 样本不足，retest |
| dynamic `history_365` | 2025-01-01 -> 2025-06-01 | 11 | 11 | 0.25 | 0.24 | -7.74% | -7.77% | 样本不足，retest |
| dynamic `history_365` | 2025-01-01 -> 2025-06-01，source-limit 100 / max-symbols 30 | 14 | 13 | 0.20 | 0.21 | -10.37% | -9.44% | 样本不足，retest |
| dynamic `history_365` | 2025-01-01 -> 2025-09-01，source-limit 100 / max-symbols 30 | 33 | 29 | 0.43 | 0.65 | -15.27% | -7.84% | 样本充足，promising retest |
| dynamic `pump_chase_strict` | 2025-01-01 -> 2025-09-01，source-limit 100 / max-symbols 30 | 33 | 33 | 0.43 | 0.43 | -15.27% | -15.27% | 样本充足，无差异 |
| dynamic `liquidity_50m` | 2025-01-01 -> 2025-09-01，source-limit 100 / max-symbols 30 | 33 | 30 | 0.43 | 0.65 | -15.27% | -8.13% | 样本充足，promising retest |
| dynamic `history_365` | 2025-09-01 -> 2026-06-01，source-limit 100 / max-symbols 30 | 20 | 20 | 0.45 | 0.45 | -10.09% | -10.09% | 样本充足，跨段无改善 |
| dynamic `liquidity_50m` | 2025-09-01 -> 2026-06-01，source-limit 100 / max-symbols 30 | 20 | 19 | 0.45 | 0.48 | -10.09% | -8.23% | 方向改善但样本差 1 笔，retest |
| dynamic `liquidity_50m` | 2025-06-01 -> 2026-06-01，source-limit 100 / max-symbols 30 | 36 | 38 | 0.72 | 0.81 | -8.77% | -5.53% | 样本充足，方向继续改善 |
| dynamic `liquidity_50m` walk-forward | 2025-01-01 -> 2025-06-01；2025-06-01 -> 2026-06-01，非重叠，source-limit 100 / max-symbols 30 | 14 / 36 | 12 / 38 | 0.20 / 0.72 | 0.23 / 0.81 | -10.37% / -8.77% | -8.51% / -5.53% | 2 段覆盖无重叠，但仅 1 段样本充足，retest |
| dynamic `liquidity_50m` | 2025-06-01 -> 2026-06-01，source-limit 150 / max-symbols 40 | 55 | 56 | 0.70 | 0.75 | -13.04% | -10.31% | 扩大 universe 后样本充足且方向继续改善，但仍为 retest |
| dynamic `liquidity_50m` fixed-master walk-forward | 2025-01-01 -> 2025-06-01；2025-06-01 -> 2026-06-01，`dynamic_master_source150.json` / max-symbols 40 | 19 / 55 | 16 / 56 | 0.29 / 0.70 | 0.35 / 0.75 | -11.23% / -13.04% | -9.09% / -10.31% | 固定 master 下近端段样本充足且改善，整体仍 retest |
| dynamic `liquidity_50m` full-master | 2025-06-01 -> 2026-06-01，`dynamic_master_full.json` / max-symbols 40 | 49 | 51 | 0.73 | 0.85 | -10.62% | -6.11% | 不截断 master 下样本充足且改善延续，但仍为负收益，继续 retest |
| dynamic `liquidity_50m` full-master walk-forward | 2025-01-01 -> 2025-06-01；2025-06-01 -> 2026-06-01，`dynamic_master_full.json` / max-symbols 40 | 17 / 49 | 17 / 51 | 0.33 / 0.73 | 0.33 / 0.85 | -11.80% / -10.62% | -10.31% / -6.11% | 早期段仍样本不足，近端段样本充足且改善，整体 retest |
| dynamic `liquidity_50m` full-master extended | 2025-01-01 -> 2025-09-01，`dynamic_master_full.json` / max-symbols 40 | 42 | 41 | 0.58 | 0.69 | -13.17% | -9.60% | 延长早期窗口后样本充足且改善延续，但仍为负收益，继续 retest |
| `liquidity_50m` full-master regime breakdown | 2025-01-01 -> 2025-09-01，baseline `1c1bd1b7b9ad` vs variant `4dae110c062c` | RISK_OFF 10 / RISK_ON 29 | RISK_OFF 9 / RISK_ON 29 | RISK_OFF 0.29 / RISK_ON 0.51 | RISK_OFF 0.32 / RISK_ON 0.64 | RISK_OFF -620.53 / RISK_ON -1123.23 | RISK_OFF -531.32 / RISK_ON -800.96 | 流动性门槛在主要亏损 regime 中都减亏，但仍不能转正 |
| dynamic `risk_off_no_core_buy` full-master | 2025-01-01 -> 2025-09-01，`dynamic_master_full.json` / max-symbols 40 | 42 | 37 | 0.58 | 0.71 | -13.17% | -7.96% | RISK_OFF 交易降到 0 并明显减亏，但 RISK_ON 略恶化，继续 retest |
| dynamic `top_n_3` full-master | 2025-01-01 -> 2025-09-01，`dynamic_master_full.json` / max-symbols 40 | 42 | 39 | 0.58 | 0.84 | -13.17% | -3.63% | RISK_ON 净 PnL 从 -1123.23 改善到 -212.97，但 RISK_OFF 不变，继续 retest |
| dynamic `risk_off_no_core_top_n_3` full-master | 2025-01-01 -> 2025-09-01，`dynamic_master_full.json` / max-symbols 40 | 42 | 35 | 0.58 | 1.00 | -13.17% | +1.04% | 首个 full master extended 转正组合，RISK_OFF=0 且 RISK_ON 大幅减亏；需非重叠 walk-forward |
| dynamic `risk_off_no_core_top_n_3` full-master early WF | 2025-01-01 -> 2025-06-01，`dynamic_master_full.json` / max-symbols 40 | 17 | 13 | 0.33 | 0.49 | -11.80% | -8.03% | 方向改善但 variant 样本不足；RISK_OFF 清零，RISK_ON 仍全止损，继续 retest |
| dynamic `risk_off_no_core_top_n_3` full-master near WF | 2025-06-01 -> 2026-06-01，`dynamic_master_full.json` / max-symbols 40 | 49 | 46 | 0.73 | 0.88 | -10.62% | -5.36% | 样本充足且继续减亏，但仍为负收益，继续 retest |
| dynamic `entry_reclaim_close` full-master | 2025-01-01 -> 2025-09-01，`dynamic_master_full.json` / max-symbols 40 | 42 | 41 | 0.58 | 0.91 | -13.17% | -3.50% | RISK_ON 净 PnL 从 -1123.23 改善到 -121.79，但整体仍为负，继续 retest |
| dynamic `entry_reclaim_close` full-master near WF | 2025-06-01 -> 2026-06-01，`dynamic_master_full.json` / max-symbols 40 | 49 | 51 | 0.73 | 1.14 | -10.62% | +5.34% | 近端样本充足且转正，RISK_ON 净 PnL -458.33 -> +627.20，需补早期段 |
| dynamic `entry_reclaim_close` full-master early WF | 2025-01-01 -> 2025-06-01，`dynamic_master_full.json` / max-symbols 40 | 17 | 18 | 0.33 | 0.31 | -11.80% | -11.36% | 早期样本不足，净收益和回撤小幅改善但 PF 略降，继续 retest |
| dynamic `entry_reclaim_close` full-master non-overlap summary | 2025-01-01 -> 2025-06-01；2025-06-01 -> 2026-06-01 | 17 / 49 | 18 / 51 | 0.33 / 0.73 | 0.31 / 1.14 | -11.80% / -10.62% | -11.36% / +5.34% | 2 段无重叠，仅近端样本充足且转正，整体仍为 retest |
| dynamic `risk_off_no_core_entry_reclaim` full-master | 2025-01-01 -> 2025-09-01，`dynamic_master_full.json` / max-symbols 40 | 42 | 38 | 0.58 | 1.03 | -13.17% | -0.03% | `RISK_OFF` closed 10 -> 0，`RISK_ON` 大幅减亏但仍为负，继续 retest |
| dynamic `risk_off_no_core_entry_reclaim` early WF | 2025-01-01 -> 2025-06-01，`dynamic_master_full.json` / max-symbols 40 | 17 | 15 | 0.33 | 0.41 | -11.80% | -8.17% | 早期样本不足，`RISK_OFF` 清零但 `RISK_ON` 仍全止损，继续 retest |
| dynamic `risk_off_no_core_entry_reclaim` near WF | 2025-06-01 -> 2026-06-01，`dynamic_master_full.json` / max-symbols 40 | 49 | 46 | 0.73 | 1.20 | -10.62% | +5.96% | 近端样本充足且转正，`RISK_ON` 和 `NEUTRAL` 改善，继续汇总判断 |
| dynamic `risk_off_no_core_entry_reclaim` non-overlap summary | 2025-01-01 -> 2025-06-01；2025-06-01 -> 2026-06-01 | 17 / 49 | 15 / 46 | 0.33 / 0.73 | 0.41 / 1.20 | -11.80% / -10.62% | -8.17% / +5.96% | 2 段无重叠，仅近端样本充足，整体仍为 retest |

当前观察：这批配置类实验在 BTC/ETH/SOL 和 13 个显式 symbols 上几乎没有区分度。Dynamic Universe Backtest MVP 已经能跑通更接近选币流程的 A/B，但在 `--source-limit 60` 下交易样本仍不足。实现 K 线无数据负缓存后，`source-limit 100 / max-symbols 30` 的 `history_365` 扩大到 `2025-01-01 -> 2025-09-01` 后首次样本充足：variant 将 PF 从 0.431 提升到 0.654，净收益从 -15.27% 改善到 -7.84%，最大回撤从 19.84% 降到 15.99%。但 `history_365` 在 `2025-09-01 -> 2026-06-01` 近端跨段中样本充足却无改善，因此不能 keep。`pump_chase_strict` 在同一窗口完全无差异，说明当前追高扣分参数没有实际筛选效果。`liquidity_50m` 目前是最强候选：`2025-01-01 -> 2025-09-01` 与 `2025-06-01 -> 2026-06-01` 两个充足样本窗口均改善 PF、净收益和回撤；短切片 `2025-09-01 -> 2026-06-01` 也方向改善但 variant closed_trades=19。`abtest-summary` v3 显示这 3 个窗口 `total_period_days=881`、`unique_coverage_days=516`、`overlap_periods=2`，因此重叠证据不能视为独立 walk-forward。`liquidity_50m` 已成为下一轮非重叠 walk-forward 或更大 universe 的优先验证对象。没有任何配置可以合并默认策略。

最新观察：`liquidity_50m` 非重叠 walk-forward 已跑通，覆盖 `2025-01-01 -> 2025-06-01` 与 `2025-06-01 -> 2026-06-01`，`unique_coverage_days=516`、`overlap_periods=0`。早期窗口 baseline/variant closed_trades 为 14/12，样本不足；近端窗口 baseline/variant closed_trades 为 36/38，样本充足且 variant 继续改善 PF、净收益和最大回撤。因此当前结论仍为 `retest`，下一步不应 keep，而应扩大 dynamic universe 或引入历史/退市 symbol master，确认结果不依赖当前 `exchangeInfo` 前 100 个 symbol。

最新进展：`liquidity_50m` 已扩大到 `source-limit 150 / max-symbols 40` 做 `2025-06-01 -> 2026-06-01` 复测，baseline/variant closed_trades 为 55/56，variant 继续改善 PF、净收益和最大回撤。这个结果加强了“提高流动性门槛可能有帮助”的证据，但 v6 总汇总仍有 `overlap_periods=5` 且 6/6 periods 使用 `source_limit`，所以结论仍是 `retest`，不能 keep。

工程进展：dynamic universe 已支持保存/加载固定 `SymbolMaster` JSON。后续可以先用 `--write-symbol-master` 固化本次 master，再用 `--symbol-master-file` 对不同 A/B 实验或 walk-forward period 复跑，降低当前 `exchangeInfo` 快照漂移对实验归因的干扰。

工程进展：新增 `dynamic-symbol-master` 独立导出命令，并已生成 `reports/2026-06-09/dynamic_master_source150.json`。后续复测 `liquidity_50m` 时优先使用该固定 master 文件。

最新实验：使用 `dynamic_master_source150.json` 复跑 `liquidity_50m` 非重叠 walk-forward 后，`2025-01-01 -> 2025-06-01` 的 variant closed_trades=16，仍低于样本线；`2025-06-01 -> 2026-06-01` 的 variant closed_trades=56，样本充足且继续改善 PF、净收益和回撤。v7 汇总 `overlap_periods=0`，但因早期段样本不足和 `source_limit` 风险仍为 `retest`。

工程进展：已导出不使用 `--source-limit` 的 full dynamic `SymbolMaster`：`reports/2026-06-09/dynamic_master_full.json`，当前包含 418 个 Binance 现货 USDT symbols。下一步优先用这份 full master 复跑 `liquidity_50m`，确认改善是否仍能在未截断 universe 下延续。

最新实验：使用 `dynamic_master_full.json` 复跑 `liquidity_50m` 近端窗口 `2025-06-01 -> 2026-06-01` 后，baseline/variant closed_trades 为 49/51，样本充足；variant 将 PF 从 0.734 提升到 0.852，净收益从 -10.62% 改善到 -6.11%，最大回撤从 24.24% 降到 21.32%。这说明 `liquidity_50m` 的改善不只来自 `source_limit` 截断样本，但策略整体仍为负收益，且还缺 full master 非重叠 walk-forward，因此继续 `retest`，不能 keep。

最新实验：full master 早期窗口 `2025-01-01 -> 2025-06-01` 已复跑，baseline/variant closed_trades 均为 17，仍低于样本线；variant 净收益 -11.80% -> -10.31%，最大回撤 14.49% -> 13.47%，但 PF 几乎不变。结合近端窗口，full master 非重叠结论仍是 `retest`：方向延续，但早期段样本不足，且策略总体负收益。

最新实验：将 full master 早期窗口延长到 `2025-01-01 -> 2025-09-01` 后，baseline/variant closed_trades 为 42/41，样本充足；variant 将 PF 从 0.579 提升到 0.693，净收益从 -13.17% 改善到 -9.60%，最大回撤从 19.43% 降到 18.81%。这进一步确认 `liquidity_50m` 在 full master 下有稳定改善方向，但所有窗口仍为负收益，不能 keep；下一步应做市场环境分层或进入入场/退出规则优化。

最新分析：新增 `backtest-regime-breakdown` 命令，并对 full master extended 的 baseline `1c1bd1b7b9ad` 与 variant `4dae110c062c` 做市场环境分层。`NEUTRAL` 只有 3 笔闭合交易且盈利，不足以归因；主要亏损来自 `RISK_ON` 和 `RISK_OFF`。`liquidity_50m` 在 `RISK_ON` 中将净 PnL 从 -1123.23 改善到 -800.96、PF 从 0.51 提升到 0.64；在 `RISK_OFF` 中将净 PnL 从 -620.53 改善到 -531.32、PF 从 0.29 提升到 0.32。结论：流动性门槛能减亏但无法解决核心负收益，下一步应针对不同 regime 设计入场/退出规则，而不是继续只加流动性门槛。

工程进展：新增 `risk_off_no_core_buy` 配置实验和 `[analysis].risk_off_core_buy_enabled` 开关，默认保持旧行为，variant 在 `RISK_OFF` 下不再允许 BTC/ETH 作为核心币豁免生成 `BUY_CANDIDATE`。下一步用 `dynamic_master_full.json` 运行 full master A/B，确认暂停弱市核心币新开仓是否能减少 `RISK_OFF` 亏损。

最新实验：`risk_off_no_core_buy` 已用 `dynamic_master_full.json` 跑完 `2025-01-01 -> 2025-09-01` full master A/B。variant 将 closed_trades 从 42 降到 37、PF 从 0.579 提升到 0.707、净收益从 -13.17% 改善到 -7.96%、最大回撤从 19.43% 降到 15.03%。分层显示 `RISK_OFF` 闭合交易从 10 降到 0，移除了 -620.53 USDT 亏损；但 `RISK_ON` 净 PnL 从 -1123.23 恶化到 -1243.74。因此该规则只能作为 promising retest，不能 keep，下一步优先处理 `RISK_ON` 入场/退出质量。

工程进展：新增 `top_n_3` 配置实验，将 `market.top_n` 从 5 降到 3，用于测试 RISK_ON 亏损簇是否来自同一 4h 决策点/同日过多相关候选同时开仓。该实验不改默认配置，下一步用 full master extended 窗口只跑 variant 并与 baseline `1d0037a773ff` 对比。

最新实验：`top_n_3` 已用 full master extended 窗口完成 A/B。variant 将 closed_trades 从 42 降到 39、PF 从 0.579 提升到 0.837、净收益从 -13.17% 改善到 -3.63%，但最大回撤仍为 19.43%。分层显示 `RISK_ON` closed_trades 从 29 降到 26，PF 从 0.51 提升到 0.88，净 PnL 从 -1123.23 改善到 -212.97；`RISK_OFF` 完全不变。因此 `top_n_3` 与 `risk_off_no_core_buy` 是互补候选，下一步应做组合实验，而不是单独 keep。

工程进展：新增 `risk_off_no_core_top_n_3` 组合实验，同时覆盖 `analysis.risk_off_core_buy_enabled=false` 和 `market.top_n=3`。下一步复用 baseline `1d0037a773ff` 跑组合 variant，并用 regime breakdown 验证 `RISK_OFF` 与 `RISK_ON` 是否分别改善。

最新实验：`risk_off_no_core_top_n_3` 组合实验已跑完 full master extended 窗口。variant closed_trades=35，PF=1.0005，净收益=+1.04%，最大回撤=15.96%，是目前第一个在 `dynamic_master_full.json` 的充足样本窗口中转正的组合。分层显示 `RISK_OFF` closed_trades 10 -> 0，净 PnL -620.53 -> 0；`RISK_ON` closed_trades 29 -> 28，PF 0.51 -> 0.82，净 PnL -1123.23 -> -387.41。结论：promising retest，但不能 keep；下一步必须做 full master 非重叠 walk-forward。

最新 walk-forward：`risk_off_no_core_top_n_3` 的早期非重叠段 `2025-01-01 -> 2025-06-01` 已跑完。variant closed_trades=13，低于样本线；PF 0.327 -> 0.488，净收益 -11.80% -> -8.03%，最大回撤 14.49% -> 11.46%。分层显示 `RISK_OFF` closed_trades 8 -> 0，但 `RISK_ON` closed_trades 7 -> 7 且仍全部止损。结论：方向改善但样本不足，继续 `retest`，还需要近端非重叠段。
最新 walk-forward：`risk_off_no_core_top_n_3` 的近端非重叠段 `2025-06-01 -> 2026-06-01` 已跑完。variant closed_trades=46，样本充足；PF 0.734 -> 0.876，净收益 -10.62% -> -5.36%，最大回撤 24.24% -> 21.38%，暴露 88.45% -> 63.01%。分层显示 `RISK_OFF` closed_trades 9 -> 1，亏损明显收缩；`RISK_ON` PF 0.84 -> 0.86，改善很小。结论：组合规则跨窗口有减亏价值，但近端仍为负收益，继续 `retest`；下一步应做两段 walk-forward 汇总，并优先研究 `RISK_ON` 入场/退出质量。
最新汇总：`risk_off_no_core_top_n_3` 已生成两份汇总。v1 包含 full master extended 与两段子窗口，因此 `overlap_periods=2`；v2 使用 `--drop-overlap-periods` 后只保留 `2025-01-01 -> 2025-06-01` 和 `2025-06-01 -> 2026-06-01`，`overlap_periods=0`、`unique_coverage_days=516`、`sufficient_periods=1`。结论仍为 `retest`：组合规则是当前最明确的减亏候选，但早期段样本不足且近端段仍为负收益，不应 keep；下一轮优先优化 `RISK_ON` 入场/退出，而不是继续叠加防守过滤。
工程进展：已实现 `entry_reclaim_close` 入场确认实验。默认配置保持关闭；variant 要求 WATCHING 计划触碰入场区后，4h 收盘价重新站上 `entry_high` 才允许实际入场。该实验用于验证 `RISK_ON` 中的接飞刀/弱反弹失败是否能通过延迟确认减少。
最新实验：`entry_reclaim_close` 已跑完 full master extended 窗口。variant closed_trades=41，样本充足；PF 0.579 -> 0.905，净收益 -13.17% -> -3.50%，最大回撤 19.43% -> 17.63%。分层显示 `RISK_ON` closed_trades 29 -> 29，PF 0.51 -> 0.94，净 PnL -1123.23 -> -121.79，stop_rate 82.76% -> 72.41%；`RISK_OFF` 仅小幅改善。结论：这是有效的 `RISK_ON` 入场质量候选，但仍未转正，继续 `retest`，下一步做非重叠 walk-forward。
最新 walk-forward：`entry_reclaim_close` 的近端段 `2025-06-01 -> 2026-06-01` 已跑完。variant closed_trades=51，样本充足；PF 0.734 -> 1.142，净收益 -10.62% -> +5.34%，最大回撤 24.24% -> 15.90%。分层显示 `RISK_ON` closed_trades 36 -> 36，PF 0.84 -> 1.23，净 PnL -458.33 -> +627.20；`NEUTRAL` 也转正，`RISK_OFF` 仍为负且略恶化。结论：入场确认是当前最强的 `RISK_ON` 改善候选，但仍需早期非重叠段验证。
最新 walk-forward：`entry_reclaim_close` 的早期段 `2025-01-01 -> 2025-06-01` 已补跑。variant closed_trades=18，仍低于样本线；PF 0.327 -> 0.305，净收益 -11.80% -> -11.36%，最大回撤 14.49% -> 13.50%。分层显示 `RISK_ON` closed_trades 7 -> 8 且仍全止损，`RISK_OFF` 基本不变。非重叠汇总保留早期段和近端段，`overlap_periods=0`、`sufficient_periods=1`，结论仍为 `retest`。下一步优先验证 `entry_reclaim_close + risk_off_no_core_buy` 是否能把近端 `RISK_ON` 转正和弱市停开核心币组合起来；若组合仍不稳，再进入 TP1 后保护性退出 A/B。
工程进展：已实现组合实验 `risk_off_no_core_entry_reclaim`，默认配置不变，variant 同时开启 `entry_reclaim_close` 并关闭 `RISK_OFF` 核心币新开仓。下一步用 `dynamic_master_full.json` 跑 `2025-01-01 -> 2025-09-01` full master A/B，并用 regime breakdown 验证 `RISK_OFF` 是否清零、`RISK_ON` 是否保留入场确认收益。
最新实验：`risk_off_no_core_entry_reclaim` 已跑完 full master extended 窗口。variant closed_trades=38，样本充足；PF 0.579 -> 1.025，净收益 -13.17% -> -0.03%，最大回撤 19.43% -> 15.11%。分层显示 `RISK_OFF` closed_trades 10 -> 0，净 PnL -620.53 -> 0；`RISK_ON` closed_trades 29 -> 31，PF 0.51 -> 0.86，净 PnL -1123.23 -> -320.18。结论：组合接近打平，是当前最强组合候选，但 `RISK_ON` 未转正且仍是单个 extended 窗口，必须做非重叠 walk-forward。
最新 walk-forward：`risk_off_no_core_entry_reclaim` 早期段 `2025-01-01 -> 2025-06-01` 已跑完。variant closed_trades=15，样本不足；PF 0.327 -> 0.413，净收益 -11.80% -> -8.17%，最大回撤 14.49% -> 10.85%。分层显示 `RISK_OFF` closed_trades 8 -> 0，但 `RISK_ON` closed_trades 7 -> 9 且仍全止损，说明早期改善主要来自弱市停开核心币，入场确认没有解决早期 `RISK_ON`。下一步仍需跑近端段，再做非重叠汇总。
最新 walk-forward：`risk_off_no_core_entry_reclaim` 近端段 `2025-06-01 -> 2026-06-01` 已跑完。variant closed_trades=46，样本充足；PF 0.734 -> 1.204，净收益 -10.62% -> +5.96%，最大回撤 24.24% -> 14.46%。分层显示 `RISK_ON` 净 PnL -458.33 -> +289.35，`NEUTRAL` -40.41 -> +533.91，`RISK_OFF` closed_trades 9 -> 1。非重叠汇总 `overlap_periods=0`、`sufficient_periods=1`，结论仍为 `retest`：组合是当前最强候选，但早期段样本不足且 `RISK_ON` 早期仍全止损，不能 keep。
工程进展：已实现 `tp1_breakeven_stop` 退出实验。默认配置保持关闭；variant 在 TP1 命中后将止损移动到入场价，用于验证保护性退出是否能减少盈利后回吐。下一步先跑 full master extended 窗口，再决定是否做非重叠 walk-forward。
最新实验：`tp1_breakeven_stop` 已跑完 full master extended 窗口。variant closed_trades=48，样本充足；PF 0.579 -> 0.546，净收益 -13.17% -> -14.90%，最大回撤 19.43% -> 19.43%，stop_rate 80.95% -> 83.33%。分层显示 `RISK_ON` closed_trades 29 -> 35，PF 0.51 -> 0.47，净 PnL -1123.23 -> -1318.06。结论：`reject_candidate`，不做 walk-forward；简单 TP1 后保本会增加噪音止损，下一轮退出应测试更柔性的 EMA20/ATR 跟踪或收盘确认后再抬止损。
最新进展（2026-06-11）：完成 sensitive 组合激活、市值分层 walk-forward、max_holding 三阈值 walk-forward、`large_cap_only_risk_off` 实验。sensitive 组合（三项规则 + 收紧 regime 阈值）已写入 `settings.toml` 并于 2026-06-11 生效，模拟盘观察期至 2026-07-02。市值分层两段 walk-forward 确认 large-cap 熊市守正、altcoin 是主要拖累（`candidate_keep_review`）。max_holding 三阈值（18/30/42根）两段全部改善、方向稳健，42根最平衡（`candidate_keep_review`）。`large_cap_only_risk_off` 实验在全量 altcoin 组合上叠加 BNB/SOL 豁免，近端反而变差，结论 `retest`，暂不 keep。当前最强路线：sensitive 组合观察期结束后决定是否叠加 `max_holding_42x4h` 规则。

## 未完成模块

### P1：策略研究和优化

- [x] 跑完 `history_365` 首段 A/B 实验。
- [x] 跑完 `pump_chase_strict` 首段 A/B 实验。
- [x] 跑完 `liquidity_50m` 首段 A/B 实验。
- [x] 对 `history_250` 做跨时段复测。
- [x] 用 13 个 symbols 池重跑配置类 A/B；结果仍无差异，判断需要动态 universe 回测。
- [x] 用 dynamic universe 重跑 `history_365`、`pump_chase_strict`、`liquidity_50m`；链路可用，但样本仍不足，全部保留为 `retest`。
- [x] 用充足样本 dynamic universe 复测 `history_365`、`pump_chase_strict`、`liquidity_50m`；`history_365` 和 `liquidity_50m` 为 promising retest，`pump_chase_strict` 无差异。
- [x] 对 `history_365` 做近端跨段 dynamic universe 复测；样本充足但无改善，暂不 keep。
- [x] 对 `liquidity_50m` 做近端跨段 dynamic universe 复测；方向延续但 variant closed_trades=19，继续 retest。
- [x] 增加 A/B 多时段汇总报告；`history_365` 和 `liquidity_50m` 当前汇总结论均为 `retest`。
- [x] 对 `liquidity_50m` 做更长近端窗口复测；样本充足且方向继续改善，但仍需 walk-forward 或更大 universe。
- [x] 增加 A/B walk-forward 编排命令，后续可用一条命令跑多时段并自动汇总。
- [x] A/B 汇总增加时段重叠分析，避免把重叠窗口误判为独立证据。
- [x] A/B 汇总增加 dynamic universe 偏差提示，避免忽略当前 `exchangeInfo` master 和 `source_limit` 截断风险。
- [x] 对 `liquidity_50m` 做非重叠 walk-forward 验证；2 段覆盖无重叠，但只有 1 段样本充足，继续 `retest`。
- [x] 对 `liquidity_50m` 做 `source-limit 150 / max-symbols 40` 扩大 universe 复测；样本充足且方向继续改善，但仍需继续 `retest`。
- [x] 支持固定 dynamic `SymbolMaster` JSON，便于复现实验 universe。
- [x] 增加 `dynamic-symbol-master` 独立导出命令，并生成 `dynamic_master_source150.json`。
- [x] 用固定 `dynamic_master_source150.json` 复跑 `liquidity_50m` 非重叠 walk-forward；整体仍为 `retest`。
- [x] 导出不截断的 full dynamic `SymbolMaster`，生成 `dynamic_master_full.json`，当前包含 418 个 symbols。
- [x] 用 `dynamic_master_full.json` 复跑 `liquidity_50m` 近端窗口；样本充足且改善延续，但仍为 `retest`。
- [x] 用 `dynamic_master_full.json` 复跑 `liquidity_50m` 非重叠 walk-forward；早期段仍样本不足，整体仍为 `retest`。
- [x] 延长 full master 早期窗口到 `2025-01-01 -> 2025-09-01`；样本充足且改善延续，但仍为 `retest`。
- [x] 增加 `backtest-regime-breakdown` 命令，并对 full master `liquidity_50m` extended 结果做市场环境分层。
- [x] 实现 `risk_off_no_core_buy`，用于测试 `RISK_OFF` 下暂停 BTC/ETH 核心币新开仓。
- [x] 跑完 `risk_off_no_core_buy` full master A/B，并生成 A/B 报告与 regime breakdown。
- [x] 实现 `top_n_3`，用于测试降低候选容量能否改善 `RISK_ON` 拥挤开仓。
- [x] 跑完 `top_n_3` full master A/B，并生成 A/B 报告与 regime breakdown。
- [x] 实现 `risk_off_no_core_top_n_3` 组合实验。
- [x] 运行 `risk_off_no_core_top_n_3` full master A/B，并生成 A/B 报告与 regime breakdown。
- [x] 对 `risk_off_no_core_top_n_3` 做 full master 非重叠 walk-forward 第一段。
- [x] 对 `risk_off_no_core_top_n_3` 做 full master 非重叠 walk-forward 第二段。
- [x] 汇总 `risk_off_no_core_top_n_3` 两段 full master 非重叠 walk-forward。
- [x] 设计 `RISK_ON` 入场确认 A/B：`entry_reclaim_close`。
- [x] 运行 `entry_reclaim_close` full master A/B，并生成 regime breakdown。
- [x] 对 `entry_reclaim_close` 做 full master 非重叠 walk-forward 近端段。
- [x] 对 `entry_reclaim_close` 做 full master 非重叠 walk-forward 早期段。
- [x] 汇总 `entry_reclaim_close` full master 非重叠 walk-forward。
- [x] 设计 `entry_reclaim_close + risk_off_no_core_buy` 组合 A/B。
- [x] 运行 `risk_off_no_core_entry_reclaim` full master A/B。
- [x] 对 `risk_off_no_core_entry_reclaim` 做 full master 非重叠 walk-forward 早期段。
- [x] 对 `risk_off_no_core_entry_reclaim` 做 full master 非重叠 walk-forward 近端段。
- [x] 汇总 `risk_off_no_core_entry_reclaim` full master 非重叠 walk-forward。
- [x] 扩大 `risk_off_no_core_entry_reclaim` 早期验证样本：补跑 `2024-07-01 -> 2025-06-01` 早期段，verdict=`candidate_keep_review`，已写入 sensitive 组合并生效于模拟盘。
- [x] 设计 `RISK_ON` 退出质量 A/B：`tp1_breakeven_stop`。
- [x] 运行 `tp1_breakeven_stop` full master A/B。
- [x] 设计更柔性的 TP1 后退出 A/B：`tp1_ema20_trailing_stop`，以及三项组合 `risk_off_no_core_entry_reclaim_ema_stop`。
- [x] 运行三项组合两段 walk-forward，verdict=`candidate_keep_review`，已激活为 sensitive 默认配置。
- [x] 市值分层两段 walk-forward：large-cap 两段正收益，altcoin 熊市严重拖累，结论 `candidate_keep_review`。
- [x] max_holding 三阈值（18/30/42根）两段 walk-forward：三阈值全部改善，42根最平衡，结论 `candidate_keep_review`。
- [x] 实现 `large_cap_only_risk_off` 实验并跑两段 walk-forward：两段方向相反，结论 `retest`，暂不 keep。
- [ ] 将 `max_holding_42x4h_no_tp1` 与 sensitive 组合合并，验证叠加效果。
- [ ] 对 A/B 报告写人工结论；当前第一批均应暂记 `retest`，不能 keep。
- [ ] 实现三段式历史结构。
- [ ] 实现趋势相关高波动惩罚实验。
- [ ] 将被 keep 的规则单独合并到默认配置。

### P1：回测增强

- [x] Universe snapshot 回测（当前快照选币 + 历史回放）。
- [x] Dynamic Universe Backtest MVP（每日用当时可得历史 K 线重建 universe 后再选币）。
- [x] 优化无历史新币的负缓存，避免动态回测 smoke 中重复请求无数据区间。
- [x] 在实现无历史新币负缓存后，扩大 dynamic-universe A/B 的 `source-limit`、`max-symbols` 和时间段。
- [ ] 对 promising 的 dynamic-universe 结果做跨时段复测。
- [ ] 引入历史/退市币 symbol master，降低当前 `exchangeInfo` 带来的退市幸存者偏差。
- [x] Walk-forward A/B 编排。
- [x] Walk-forward 时段重叠覆盖分析。
- [x] A/B 汇总 Universe Bias Checks。
- [x] `liquidity_50m` 非重叠 walk-forward 复测。
- [x] `liquidity_50m` 扩大到 `source-limit 150 / max-symbols 40` 复测。
- [x] 保存/加载 dynamic symbol master。
- [x] 独立导出 dynamic symbol master。
- [x] 用固定 `SymbolMaster` 文件复跑 `liquidity_50m` 非重叠 walk-forward。
- [ ] 移除或继续提高 `--source-limit` 复测 `liquidity_50m`。
- [ ] Walk-forward 市场环境分层分析。
- [ ] 牛市、熊市、震荡、暴跌、周末分层统计。
- [x] 回测报告增加 A/B 多时段汇总。
- [ ] 更细 intrabar 数据裁决，例如 15m 或 5m。

### P1：报告和复盘

- [ ] A/B 报告写入 Obsidian `Reports/YYYY-MM-DD/`。
- [ ] 增加实验结论索引页。
- [ ] 增加交易复盘模板。
- [ ] 增加每周策略复盘笔记模板。
- [ ] 增加策略参数变更历史表。

### P1：监控和提醒

- [ ] Telegram 或邮件提醒。
- [ ] 大盘风险变化提醒。
- [ ] 模拟盘入场、止损、止盈触发提醒。
- [ ] API 异常或数据源异常提醒。

### P1：Dashboard

- [ ] 简单 Web 仪表盘。
- [ ] 当前候选币视图。
- [ ] 当前模拟盘持仓视图。
- [ ] 回测和 A/B 实验结果视图。
- [ ] 收益曲线和回撤曲线。

### P2：交易执行和实盘准备

- [ ] 手动确认后下单。
- [ ] 交易所 API Key 权限隔离。
- [ ] 只读 / 交易权限分离。
- [ ] 实盘开关。
- [ ] 最大单笔下单额限制。
- [ ] 最大日亏损熔断。
- [ ] 自动撤单和订单重试。
- [ ] 交易所故障处理。

### P2：多交易所和更多数据源

- [ ] OKX / Bybit 行情对照。
- [ ] 资金费率。
- [ ] Open interest。
- [ ] 稳定币流动性。
- [ ] ETF / 宏观风险指标。

## 近期 Git 记录

- `1c65d12` Update project memory rules
- `b7fbf81` Add abtest experiment runner
- `52aaec2` Update selection optimization todo
- `87bafbe` Tighten selection workflow
- `983737c` Parameterize selection penalties

## 备注

这个系统当前仍应作为"交易研究与模拟盘工具"，不是自动赚钱机器。真正有价值的是让每一次交易都有计划、有证据、有风控、有回测、有复盘。

## 2026-07-29 Stage N0 / N1 gate 更新

根据 GPT 评审和 Codex 复核，`atr_reclaim_0_35` 下一步没有直接进入第三窗口确认性 A/B，而是先完成 Stage N0 readiness audit。审计结果为 `n0_conditional_pass_with_universe_bias_warning`：代码和配置冻结是干净的，但固定 `dynamic_master_full.json` 缺少 listing dates，且第三窗口只有 207/418 symbols 三周期完整覆盖，152/418 symbols 完全没有对应历史 K 线。因此第三窗口不能作为 clean confirmatory validation，只能作为带 universe/listing-date caveat 的 diagnostic retest。

当前优先级调整为：先补 listing-date enriched `SymbolMaster` 或历史 membership 证据并重跑 N0；若在补齐前继续 N1，则必须明确标注 diagnostic，只能把结果用于判断是否继续研究，不能用于 keep、部署或修改 `settings.toml`。

## 2026-07-30 Stage N1 diagnostic retest 结论

`atr_reclaim_0_35` 第三窗口 diagnostic retest 已完成。组合层继续改善：净收益 22.10% -> 31.55%，PF 1.264 -> 1.376，MDD 21.85% -> 21.08%，Sharpe 0.932 -> 1.238。但机制层没有通过：同一 opportunity key 下 baseline 进入而 variant 未进入的 50 笔直接过滤样本本来净贡献为正，错过赢家成本大于避免亏损收益；整体改善主要来自 variant-only / same-key variant-only entered 的路径新增交易，以及 2023-11/12 和少数 symbol-month cluster。

因此当前阶段判断为 `retest_path_dependent`，不是 keep。项目路线不进入部署、不叠加过滤器、不用第三窗口继续调相邻 ATR 阈值。下一步应优先补 listing-date enriched `SymbolMaster` 或历史 membership 证据，并改进 strict opportunity id / capacity-state alignment 后重跑 N0/N1。

## 2026-07-30 Stage N2 ??????

- ?????`atr_reclaim_0_35` ???????? data-substrate gate???? `diagnostic_only_historical_membership_gap`?
- ???????? current master listing date ??????????????? historical membership ?????? `147 / 413` historical USDT symbols ?? current master?
- ????????????? clean confirmatory validation?????? N1?????????????? `atr_reclaim_0_35`?
- ???????? historical symbol membership dataset?? dynamic universe ???????????????? exchangeInfo ?????
- ?????baseline ? variant ??????? historical dynamic universe??? symbol ??? `listing_time / delisting_time / first_kline_time / last_kline_time / source / confidence`??????????????????? validation limitation?

## 2026-07-30 Stage N3 ??????

- ????????????? gate ??????? `third_window_not_recoverable_without_historical_master`?
- ???????? N2 ? `147` ? missing historical symbols ????????????????????????????? `127` ? standard-like gap?
- ??????????? corrected N1 ????????`atr_reclaim_0_35` ????????? diagnostic???? keep/reject?
- ???????? source-backed historical master???? `2023-07-01 -> 2024-07-01`??????????? walk-forward ???
- ??????? standard-like missing symbol ?? delisted / renamed / migrated / still-tradable / exclude-by-rule ????????? `source` ? `confidence`?

## 2026-07-30 Stage N4 ??????

- ?????historical master MVP ????? validation gate ????
- ??????????? universe ?????? 413 ? master rows ? 127 ? blocking review queue?
- ??????? N4 MVP ???????????????? `atr_reclaim_0_35` ?????
- ???????? official-source mapping pipeline?? blocking review queue ???????????
- ?????127 ? blocking symbols ???? `include_historical / exclude_by_rule / mapped_to_new_symbol / delisted_before_or_inside_window / unresolved`???? `source` ? `confidence`?

