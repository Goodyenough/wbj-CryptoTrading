# 2026-07-02 Paper 观察验收标准

## 核心原则

2026-07-02 的验收目标不是简单判断“这几周赚没赚钱”，而是判断：

> 当前这套 paper 观察系统是否给出了足够干净、可追溯、能改变决策的证据，用来决定现有策略是继续保留、需要小修，还是必须重新实验。

验收顺序应当是：

1. 先验收数据链路是否可信。
2. 再验收策略行为是否符合设计。
3. 最后才讨论收益表现。

如果数据链路不可信，策略结论必须降级为“样本不完整”，不能急着改参数。

## 1. 数据链路是否通过

这是最底层标准。没有这个，后续所有策略判断都不可信。

通过标准：

```text
daily 每天成功
4h update 按 00:10 / 04:10 / 08:10 / 12:10 / 16:10 成功
数据库 runs 状态为 success
paper_snapshots / paper_events 持续写入
企业微信通知包含 database: status=success
无 stale running run
config_hash 持续保持 be7ec39ec21f6a83
```

这一项的意义是确认 7 月 2 日复盘的是同一套策略、同一套配置、同一套观察口径，而不是中途混入脚本异常、数据库缺口或配置漂移。

判定：

```text
通过：
  可以继续做策略行为判断。

部分通过：
  可以阅读报告，但策略结论需要标注样本缺口。

失败：
  不做策略参数结论，先修系统链路。
```

## 2. RISK_OFF 防守机制是否通过

当前观察期里，市场多次处于 `RISK_OFF`。这套策略的设计不是强行每天找买点，而是在弱市中主动降低买入候选质量和开仓冲动。

要回答的问题是：

```text
市场差的时候，系统有没有真的收手？
```

通过标准：

```text
RISK_OFF 期间，大多数扫描结果应为 REJECT / WATCH_ONLY。
不应持续产生新的 BUY_CANDIDATE 或高风险新计划。
```

警惕标准：

```text
RISK_OFF 下连续多天仍出现 BUY_CANDIDATE。
新增计划随后快速 invalidated / stopped。
```

失败标准：

```text
RISK_OFF 下新增计划很多，且后续大面积跌破 stop 或快速失效。
```

如果 7 月 2 日前市场持续偏弱，而系统没有乱开新计划，这不是“没有机会”的坏事，而是风控机制发挥作用。

## 3. RECLAIM_PENDING 机制是否通过

`RECLAIM_PENDING` 是当前 paper 观察期最重要的机制之一。

当前逻辑是：价格触碰 entry zone 还不够，必须 4h close reclaim 到 `entry_high` 上方，才允许真正入场。否则保持观察，等待确认。

它要验证的问题是：

```text
这个 reclaim 要求是在避免坏入场，还是错过好机会？
```

通过标准：

```text
被 RECLAIM_PENDING 拦截后，价格多数继续走弱、跌破 stop、或长期不能 reclaim。
```

这说明当前机制有效避免了弱反弹中的错误入场，应保留。

需要 retest 的标准：

```text
多次出现：
价格被 RECLAIM_PENDING 拦截；
随后重新站回 entry_high；
并且继续走到 TP1 或产生明显正收益；
但系统没有及时入场。
```

失败或强 retest 标准：

```text
出现 2-3 个不同币种的明确漏单案例；
漏单后都达到 TP1 附近或产生大幅正收益；
且不是同一个 symbol / plan 的重复事件。
```

注意：不要只按事件次数判断。一个计划可能每 4 小时重复产生很多 `RECLAIM_PENDING`，但本质上仍是同一个机会。更合理的判断维度是：

```text
plan 数量
symbol 数量
是否真正 reclaim
reclaim 后是否上涨
是否达到 TP1
```

## 4. 42 根 4h 持仓规则是否通过

这是前期回测和 A/B 实验讨论最多的核心问题。

目前策略方向已经暂定为固定 42 根 4h，不继续在 `conditional=true/false` 上纠缠。原因是三窗口实验显示：条件式延长持仓有时改善，但并不是稳定强增强，在部分窗口反而变差。

paper 观察要回答的是：

```text
真实 paper 中，那些超过 42 根 4h 的仓位，后面到底是继续贡献收益，还是明显回吐？
```

支持保留固定 42 根的标准：

```text
超过 42 根后，多数仓位没有继续显著上行；
新增收益有限；
回撤扩大明显；
最终跌破 EMA、跌回入场附近、或 stopped。
```

支持重新实验更宽松持仓的标准：

```text
超过 42 根后，有多个仓位继续明显扩大利润；
如果 42 根退出，会错过主要收益段；
后续回撤仍可接受。
```

样本不足时的标准：

```text
如果只有 1-2 个样本，且结论冲突：
不改策略，只标记 retest。
```

7 月 2 日复盘时，不应问“有没有某一个超过 42 根后继续上涨的案例”，而应问：

```text
42 根之后的边际收益是否稳定大于新增回撤和持仓时间成本？
```

如果答案不清楚，就不要改默认规则。

## 5. TP1 EMA trailing stop 是否通过

这个模块验证的是：

```text
命中 TP1 后，用 EMA trailing stop 是否能保护利润，同时不太早踢出趋势单？
```

通过标准：

```text
有 TP1 后，EMA stop 能上移并保护利润；
如果出场，出场后价格没有很快大幅继续上涨；
止损抬升记录、激活记录、出场记录均能在 report 和 database 中追踪。
```

警惕标准：

```text
EMA stop 一激活就频繁把仓位洗出；
出场后价格继续大幅上行到 TP2 附近；
说明 trailing stop 可能过紧。
```

样本不足标准：

```text
如果到 2026-07-02 仍然没有 TP1 样本：
结论应写为“样本不足，不做修改，继续观察”。
```

没有 TP1 样本不是失败。没有样本时强行评价 EMA trailing stop，才是失败。

## 6. 当前策略整体是否通过

当前策略不是单纯追涨，而是一套偏防守的 paper 观察流程：

```text
daily 扫盘生成候选
RISK_OFF 降级弱市机会
paper 只跟踪计划
4h update 高频更新已有计划
entry reclaim 防止弱反弹误入场
stop 控制单笔损失
TP1 后用 EMA trailing 保护利润
42-bar review 检查持仓时间是否过长
```

总体验收可以分四档。

### keep

```text
数据完整；
RISK_OFF 下没有乱开仓；
RECLAIM_PENDING 多数证明拦截有效；
超过 42 根后的仓位没有系统性证明“应该更久持有”；
没有发现 stop / TP1 / report / database 明显逻辑错误。
```

结论：保留当前策略和配置。

### keep_observing

```text
数据完整；
系统执行正常；
但市场太弱，TP1 样本不足；
胜率和收益表现不好，但原因主要来自市场环境和防守状态。
```

结论：暂不改参数，继续等待更完整的市场环境。

### retest

```text
某个模块出现清晰机会成本；
例如多个 reclaim 漏单；
多个 42-bar 后继续大涨；
RISK_OFF 过度过滤导致完全错过强币；
TP1 EMA trailing stop 过早踢出趋势单。
```

结论：7 月 2 日后针对单一模块开新实验，不在观察期内临时改配置。

### reject / fix

```text
数据库或任务链路不稳定；
config_hash 漂移；
paper 状态流转错误；
RISK_OFF 下持续乱开仓；
止损、入场、TP1 事件记录明显不可信。
```

结论：优先修系统或规则错误，不做收益归因。

## 7 月 2 日复盘时的推荐输出

建议最终复盘写成以下格式：

```text
1. 数据链路结论：通过 / 部分通过 / 失败
2. RISK_OFF 结论：keep / retest / fix
3. RECLAIM_PENDING 结论：keep / retest / sample_insufficient
4. 42-bar 结论：keep / retest / sample_insufficient
5. TP1 EMA trailing stop 结论：keep / retest / sample_insufficient
6. 总体结论：keep / keep_observing / retest / fix
7. 下一步：只列 1-3 个最能改变决策的实验或修复
```

核心原则：

```text
不要因为几天没赚钱就改整套策略。
不要因为单个漂亮案例就改默认参数。
不要在样本不足时假装有结论。
优先相信干净、连续、可追溯的 paper 数据。
```

这三周最珍贵的不是短期收益，而是确认自动化 paper 观察系统能否提供稳定、可信、不会自欺的数据。只有在系统可信之后，策略参数实验才有意义。
